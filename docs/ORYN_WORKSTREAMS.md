# ORYN Workstreams — live ownership map

**Purpose:** prevent duplicate work across concurrent Claude agents. This file answers
*who owns what, on which branch, right now* — nothing else. It is rewritten in place per
workstream row, not appended to as a log.

**This is not a duplicate planning system.** The other three canonical documents already
exist under different names and are not reproduced here:

- **Master plan / product direction** → [`docs/MASTER-EXECUTION-STRATEGY.md`](./MASTER-EXECUTION-STRATEGY.md)
  (reset 2026-08-20 — read this first, it is current)
- **Operational status / live coverage numbers** → [`docs/current-state.md`](./current-state.md)
  (rewritten at each integration checkpoint, not appended to — check its own "last
  measured" date before trusting it; a stale copy is worse than no copy)
- **Permanent decision log** → [`docs/product-decisions.md`](./product-decisions.md)
  (autonomous-decision record; append new enduring decisions there, not here)

If you're about to create `ORYN_MASTER_PLAN.md`, `ORYN_STATUS.md`, or `ORYN_DECISIONS.md`,
stop — those roles are already filled by the files above. Only this ownership table was
genuinely missing as of 2026-08-20.

## How to use this file

Before starting a substantial new package: `git fetch --all --prune`, re-read this file,
confirm nobody already owns what you're about to do. Claim your row (update it) before
starting large work; update it again when you finish or hand off. Do not rewrite another
agent's row from assumption — verify from git log/live DB first, then correct with
evidence if it's actually wrong.

## Current rows (last updated 2026-08-22 — CANONICAL-LIVE-COLUMN row added on claim; CA-PROGRAMMES row added on close-out; all earlier rows as of 2026-08-21's non-UI integration wave — DATA-A/PROD-B/COORD/UI-SIMPLIFY rows carried from Claude B's copy, the RESEARCH row carried from Research Claude's copy since only it had the full R2.1/R3.1/R4 detail. Neither side rewritten from assumption.)

| Workstream | Owner | Branch | Current package | Scope | Status |
|---|---|---|---|---|---|
| DATA-A | Claude A | `oryn/programs-pipeline-reconciled` | Admissions-URL coverage campaign (4 batches, 512/1019 universities, 50.2%) + programme-catalogue pipeline (Delft/Trinity pilot + Edinburgh/Waterloo/Glasgow batch 2, 6 real extraction bugs found and fixed with tests) + opportunities Wave 1 groups B/C/D/F/G applied + dedup QA cleanups + **0043 data backfill run and verified (9/9 pairs correctly `duplicate_status='superseded'`)**. | University/opportunity data acquisition, canonical graph, ingestion pipelines, data QA — see `docs/MASTER-EXECUTION-STRATEGY.md` §3 "Computer A" for the full scope list | **Deferred from the 2026-08-21 non-UI integration wave** — branch was observed moving continuously throughout the integration window (10+ new pushed commits in under an hour, including code changes in `lib/acquisition/programs.ts` and two new acquisition scripts that were never independently validated). Per the freeze-test rule, a still-moving branch is not merged. Not yet merged to `main`; re-attempt once it holds still long enough to validate. |
| PROD-B | Claude B | `oryn/counselor-data-quality-v1` | B1-B12 complete (see `docs/integration-readiness-report-2026-08-20.md`) + freeze/merge-prep phase (see `docs/handoffs/claude-b-merge-plan-2026-08-20.md`) — found and fixed a real live-crash bug this pass (`refreshOpportunityMatches` 42703-erroring on any env without migration 0047). | Counselor intelligence, product/UX, opportunity data quality, canonical entity integration, `main` integration ownership per the strategy doc | Feature-frozen, stable and unchanged across two separate integration checkpoints (`4d3eb5e`). Included in the 2026-08-21 non-UI integration wave. |
| COORD | Claude B | `oryn/coordination-integration-2026-08-20` | Coordination-only package, merged to `main` as `5c59115`. | Docs only | Done — DATA-A already merged it back into their branch (`19c6fca`). |
| UI-SIMPLIFY | Claude (third session) | `oryn/ui-simplification-v1` | UX simplification — dashboard hierarchy pass, sticky profile jump-nav, citizenship settings form, opportunity-preview label fix, Discovery/Map package, Opportunity Detail Experience V1. **Has merged PROD-B's entire branch in twice** (`6bbe245`, then `1488ec4` after PROD-B's `41a5d5a`) — confirmed via `git merge-base --is-ancestor`. | UX research, cognitive-load reduction, progressive disclosure, final information architecture | **Explicitly excluded from the 2026-08-21 non-UI integration wave per founder instruction.** Continuing independently; will rebase/merge from the updated `main` and undergo its own integration gate in a later wave. Latest observed: `d87912b`. |
| (unclaimed) | — | `oryn/university-intelligence-spine`, `oryn/counselor-core-v1`, `oryn/integration-2026-08-19`, `oryn/recovery-pre-integration-2026-08-19`, `oryn/product-ux`, `oryn/programs-opportunities-intel`, `oryn/programs-pipeline` | — | No commits ahead of what's already in the DATA-A/PROD-B lineage as of the last check by either session — treat as historical/superseded unless a session actively resumes one. | Dormant unless noted. |
| RESEARCH | Research Claude | `oryn/research-turkey-schools` (isolated worktree at `.claude/worktrees/research-intelligence`, branched from `origin/main`@`5c59115`; branch name is now Wave-1-specific but the lane carries R2.1/R3.1/R4/5th package too — not renamed mid-package to avoid branch churn) | Five packages complete this checkpoint: (1) Turkey high-schools canonical registry, Wave 1 — 100/100. (2) Secondary-education-system intelligence (R2.1) — 8/8 systems, incl. a confirmed schema-modeling error (`education_records.curriculum='ap'`) flagged for Claude B. (3) Country-level admissions-system intelligence (R3.1) — 6/6 destination systems (US/UK/Netherlands/Italy/Germany/Canada), each with a dedicated Türkiye-applicant section and a 12-rule evidence-based ruleset; key finding: DSD-vs-academic-qualification-recognition conflation (Germany) is the highest-stakes counselor risk found across all 3 packages. (4) Opportunity eligibility intelligence (R4, self-directed) — grade/age normalization across 6 systems, a citizenship/residency/school-location 3-axis taxonomy across 20 real programs, prerequisite/timing patterns across 5 opportunity categories, plus a 32-row sourced extraction-audit sample against 102 live `opportunities` rows and a 14-rule evidence-based ruleset. (5) Counseling-materials discovery-list gap closure — 2 new verified opportunity candidates, 4 selectivity-evidence update proposals, and 2 unplanned data-quality findings (a university-only "Hack-AI-thon" row wrongly live as a high-school opportunity, and a likely Wharton Data Science Competition / Sports Analytics duplicate pair). | `docs/research/**`, `data/research/schools/**`, `data/research/academic-systems/**`, `data/research/admissions-systems/**`, `data/research/opportunity-eligibility/**`, `data/research/opportunities/**`, `docs/handoffs/research-*.md` — research/evidence only, no schema/app/production writes | All five packages complete and pushed; confirmed pure (zero app code/migrations/schema across its entire diff vs `main`). Included in the 2026-08-21 non-UI integration wave. Handoffs: `docs/handoffs/research-turkey-high-schools.md`, `docs/handoffs/research-secondary-education-systems.md`, `docs/handoffs/research-admissions-systems.md`, `docs/handoffs/research-opportunity-eligibility.md`, `docs/handoffs/research-opportunities-counseling-list.md` (consumers: DATA-A and PROD-B per package). This lane does not write to Supabase or production code — every output is a reviewed proposal. Live-remeasured this checkpoint: `opportunities.eligible_countries` moved from 14/334 (R2.1 checkpoint) to only 18/352 (~5.1%) — the gap has not materially closed. |
| ADMISSIONS-INTEL | Claude (overnight research session, 2026-08-21 ~01:2x–13:00+ local; continued past 11:00 once the founder directly lifted the stand-down and confirmed the coordination session's delegated authority) | `oryn/admissions-intelligence-research` (isolated worktree at `.claude/worktrees/admissions-intelligence`, branched from `main`@`5ec6700`) | R3.1 continuation/expansion, now three layers deep: (1) 15 country systems (original 6 unmodified + 8 destinations + Turkey's own domestic YKS/ÖSYM system) in `admissions-systems-v1.json`; (2) 7 program-family requirements docs (Medicine/Law/Architecture/Arts-Design/Engineering/Computer-Science/Economics-Business) covering all 15 countries each, in `docs/research/admissions-systems/program-requirements/`; (3) RULE-ADMISSIONS-001 through 021 (001-012 original, 013-017 country-pass, 018-021 program-family-pass — none renumbered); (4) an implementation-gap analysis against the actual shipped code (`docs/research/admissions-systems/implementation-gap/`) — top finding: `admissionSystemType` (the holistic-vs-credential-gate distinction) is fully built and tested but never populated by either real production caller, zero live effect today. Batch 3 (Sweden/Belgium/Austria/Poland/Czech Republic) still not started — not blocked, just not yet assigned. Full detail: `docs/handoffs/research-admissions-systems-v2.md`. | `docs/research/admissions-systems/**` + `README.md` (existing content preserved, additive only), `data/research/admissions-systems/**`, `docs/handoffs/research-admissions-systems-v2.md`. Research/evidence only, no schema/app/production writes — same posture as the RESEARCH row. | **Complete for this session's authorized scope, idle pending next assignment.** Every committed file is finished/validated, no half-written docs. Zero file-path overlap with COUNSEL-RESEARCH or any code/data lane. |
| TURKEY-OPPORTUNITIES | Claude (same overnight session as ADMISSIONS-INTEL, pivoted to this lane 2026-08-21 ~14:00+ local) | `oryn/turkey-opportunities-research` (isolated worktree at `.claude/worktrees/turkey-opportunities`, branched from `origin/main`@`7dc3019`) | Turkish-student opportunity research (research/internship/competition/volunteering/scholarship/entrepreneurship categories, ages ~14-18), assigned after two live measurements: Turkey had zero `research`/`internship`-category rows, and `community_impact`/`leadership` were nearly absent across all tagged opportunities. **24 verified records across 4 files** (`turkey_batch1-4_2026-08-21.jsonl`), each independently researched to an explicit accuracy>provenance>freshness>completeness>volume standard — official-source-only, LLM prior knowledge never treated as evidence, every date independently re-checked (caught a foundation's "extended to Oct 2" notice that was actually dated 2025), conflicts recorded not silently resolved. Real negative findings kept, not hidden: TOG confirmed university-only, AKUT Derneği's own tracks all miss 14-18 (but its legally-distinct sister foundation AKUT Vakfı has a real lise programme). One unresolved sub-lead: the gençlik meclisi/kent konseyi ex-officio civic-leadership statute (Belediye Kanunu 5393 madde 76) couldn't be confirmed against mevzuat.gov.tr primary text (unreachable, consistent with a prior lane's identical experience) — the underlying fact was independently confirmed on two municipalities' own pages instead. Full detail: `docs/research/opportunities-turkey/README.md`. | `data/research/opportunities/turkey_*.jsonl` (prefix reserved for this lane; other concurrent opportunity lanes use `night*`/`leadership_`/`thincat_` prefixes in the same shared directory), `docs/research/opportunities-turkey/**`. Research/evidence only, no schema/app/production/Supabase writes. | **Complete for this pass.** All 4 batches individually committed and pushed. Zero overlap with sibling opportunity-research lanes' own files (prefix-isolated in a shared directory, not a separate directory — checked for filename collisions before each write). |
| DE-NL-PROGRAMMES | Claude (same overnight session, pivoted to this lane 2026-08-21 ~17:30+ local) | `oryn/de-nl-programmes-research` (isolated worktree at `.claude/worktrees/de-nl-programmes`, branched from `origin/main`@`5b01d7d`) | Three-wave programme-catalogue deepening, assigned by the coordination session — **complete: 16 universities, 3,089 records.** **Wave 1 (5 universities, 890 records)**: LMU 180 (deepened from 4), Heidelberg 271, TU Berlin 142 (matches the university's own stated totals exactly), Bonn 257, Delft 40 — all fresh except LMU. Surfaced and corrected (with a peer and the coordinator) a live-data gap in the brief: RWTH Aachen/FU Berlin were an **ingestion gap** (152 records already researched on `programs-pipeline-reconciled`, verified via `git merge-base --is-ancestor`, not re-researched — since ingested) and VU Amsterdam was a **known twice-attempted technical blocker**, not untouched. **Wave 2 (5 universities, 946 records)**, per the coordinator's "Dutch deepening before Germany's honest zeros" steer: Erasmus 148, Tilburg 123 (cleared a Cloudflare JS challenge after confirming `robots.txt` permits AI use), UvA 326 (found a public JSON REST API backing a JS-rendered page), Groningen 186 (zero language-nulls, used the Dutch CROHO accreditation code as a dedup key), and **VU Amsterdam's blocker broken** — 163 records. Root cause: Azure AI Search's `search.in()` needs a delimiter argument two prior attempts omitted, so their filters were silently ignored rather than erroring; fix was intercepting a request fired live from inside the page's own JS session context rather than replaying one captured externally — flagged by the coordinator as generalizing to at least one other blocked university this research programme hit that day. **Wave 3 (6 universities, 1,253 records)**: Humboldt 321 (cleared an Anubis proof-of-work page, not a CAPTCHA), Freiburg 228, Göttingen 215, Hamburg 198, TU Darmstadt 123, Stuttgart 168 — all fresh except Humboldt's pre-existing 3. Caught and fixed one real duplicate before commit (Göttingen's "Geoscience"/"Earth and Environmental Sciences" — one programme mid-rename in the university's own backend, live-verified and merged), while correctly leaving Göttingen's 21-way Master of Education shared-hub-page cluster alone as a documented, legitimate source structure. Found a new category of data disagreement flagged by the coordinator: EUR's and TU Delft's own pages state different languages of instruction for the same joint programme — two authoritative co-owners disagreeing, not a stale-copy or scale-mismatch case. Language-of-instruction discipline held throughout: Bonn alone has 87/257 null because the source omits the field; the Dutch corpus (944 records) has only 8 nulls; wave 3 has just 4 nulls across 1,253 records. Full detail: `docs/research/university-programs-de-nl/README.md`. | `data/research/university-programs/de_nl_*.jsonl`, `docs/research/university-programs-de-nl/**`. Research/evidence only, no schema/app/production/Supabase writes. | **All three waves complete and pushed — 16 universities, 3,089 records.** Cross-checked against the entire existing research corpus, not just this lane's own files, every wave: the corpus does carry 46 pre-existing duplicate IDs / 181 duplicate URLs, all between older files unrelated to this lane, confirmed none of this lane's 16 files are involved. |
| DE-NL-REQUIREMENTS | Claude (same session, pivoted 2026-08-21 ~18:15+ local after DE-NL-PROGRAMMES closed) | `oryn/de-nl-programmes-research` (same branch/worktree as DE-NL-PROGRAMMES) | Admission-requirements and application-deadlines research for the same 16 universities, assigned by the coordinator to close the product's biggest data gap (`university_requirements` 84 rows / `university_deadlines` 26 rows against 122 universities with programme coverage) — **complete: 991 records (595 requirements + 396 deadlines) across all 16, three waves.** Central finding: 201/396 deadlines (51%) are genuinely undated (`recurring_annual_undated`, verbatim day/month, no year ever synthesised) — the rate ranges from 0% (TU Berlin, every deadline dated) to 92% (Heidelberg), proving this is a per-institution page-maintenance property, not a national one. Confirmed the Studielink 15-January/1-May national deadlines are genuinely undated on Studielink's own page, independently, six separate times across every Dutch university, cited as HIGH authority per coordination DECISION 1. Found the ETS TOEFL-rescale trap 124 times, both handled-well and handled-badly **within the same institution** in three separate cases (TU Delft's MSc page correct / BSc page not; Stuttgart's INFOTECH correct / WASTE not) — proof the defect is per-page, not per-university. Found a three-way uni-assist.de typology the brief only anticipated two of (doesn't use it at all; uses it but it explicitly defers to the university's own dates; doesn't require it but accepts its evaluation reports as fallback evidence). **13 genuine conflicts found and recorded, none resolved** — including one, Erasmus's EUC page, reconfirmed via four independent fetches before being recorded as conflicting rather than assumed-resolved, and one, Groningen's FEB deadline, where the *older-looking* page is the one that's actually correct (a "prefer the newer page" heuristic would get it wrong). Caught and fixed a real self-contradiction in this lane's own agent brief (German `cycle_year` convention) mid-run, corrected 3 still-running agents live and 1 already-completed agent's 9 records post-hoc. Full detail: `docs/research/university-requirements/de-nl-requirements-deadlines-summary.md`. | `data/research/university-requirements/de_nl_*.jsonl`, `docs/research/university-requirements/de-nl-requirements-deadlines-summary.md`. Research/evidence only, no schema/app/production/Supabase writes. | **Complete and pushed, idle pending next assignment.** All enum-constrained fields validated against exact contract vocabularies (`requirement_category_db` in particular is a live Postgres enum). Zero duplicate IDs within this lane's 32 files or against the full existing requirements/deadlines corpus, including the pre-existing 84+26 rows. |
| US-REQUIREMENTS | Claude (same session, pivoted 2026-08-21 ~19:20+ local after DE-NL-REQUIREMENTS closed) | `oryn/us-requirements-research` (new isolated worktree at `.claude/worktrees/us-requirements`, branched from `origin/main`@`b81b098` — a fresh worktree, not an extension of the DE/NL branch, since this is a genuinely different market) | US admission-requirements and application-deadlines research for 12 named universities (MIT, Stanford, Harvard, Yale, Princeton, Columbia, UC Berkeley, UCLA, Michigan-Ann Arbor, Georgia Tech, CMU, NYU), assigned as the first US-market pass in this product — **complete: 522 records (355 requirements + 167 deadlines) across all 12, two waves.** Pre-dispatch identity check against the live DB caught two real problems before any research started: "University of Missouri, Columbia" as a false-positive name match for Columbia University, and a genuine duplicate `universities` row for MIT (independently confirmed by the coordinator as one of nine such database-wide pairs, all same `canonical_entity_id` — a `universities`-table issue, not a broken entity-dedup layer). Test-optional policy — the top-flagged trap — showed up in every real shape predicted: a genuine live conflict at Harvard (5 current pages vs. one undated 2021 page with no superseding banner); two universities (Princeton, Columbia) not yet reinstated despite the brief assuming they had been, corrected by direct verification; a stale page correctly classified historical rather than conflicting at Columbia because it self-scopes to closed cycles; and Georgia Tech, which never went test-optional at all. **This lane's own briefs were wrong 5 times and caught each time**: Princeton's reinstatement timing, Columbia's nonexistent ED II, Yale's already-superseded AP/IB framing, Michigan's assumed two (not three) admission plans, and a claim that `admission.universityofcalifornia.edu` fails the source-authority gate (it doesn't — the UC Berkeley agent pulled the real `looksOfficial()` function and ran it directly). The Columbia agent's wave-1 finding (financial-aid subdomains label by enrollment year, admissions subdomains by cycle year, one year apart under an identical-looking string) recurred live three more times independently (Georgia Tech, UC Berkeley, UCLA) — now a standing check for any future US pass. **11 conflicts recorded, none resolved**, including a genuine three-way conflict at CMU across three different official sources for the same ED/RD dates. CMU's 89 records are the largest single-university batch in the whole research programme, reflecting real per-college admission differences (7 colleges, non-uniform testing policy, 11 distinct coursework tables). Full detail: `docs/research/university-requirements/us-requirements-deadlines-summary.md`. | `data/research/university-requirements/us_*.jsonl`, `docs/research/university-requirements/us-requirements-deadlines-summary.md`. Research/evidence only, no schema/app/production/Supabase writes. | **Complete and pushed, idle pending next assignment.** All enum-constrained fields validated. Zero duplicate IDs within this lane's 24 files or against the full existing requirements/deadlines corpus (DE/NL's 991 + the pre-existing rows). Zero Missouri/wrong-institution contamination confirmed via direct grep. |

| DUPLICATE-UNIVERSITIES | Claude (new lane, assigned 2026-08-22 by the coordination session) | `oryn/duplicate-universities-audit` (isolated worktree at `.claude/worktrees/duplicate-universities`, branched from `origin/main`@`b99ffe2`) | Finished what migration 0043's supersession mechanism half-did — **complete, pushed.** (1) Traced the live "two rows for MIT" leak to the acquisition layer, not the UI: 3 of 5 sibling ingestion scripts (`ingest-university-programs.ts`, `ingest-university-programs-batch.ts`, `ingest-requirements-deadlines.ts`) built their identity-resolution candidate pool from the raw `universities` table with zero supersession filtering; fixed all 5 to share one mechanism (`excludeSupersededUniversities()` pre-filtering, replacing both a hand-rolled `Set` filter and a post-hoc redirect). Verified live: zero rows in `university_programs`/`university_requirements`/`university_deadlines` reference a superseded id — no data repair needed. Added a regression test reproducing the real ambiguity end-to-end through `decideIngestion()`. (2) Resolved Al-Farabi as a duplicate, not a rename — confirmed against ROR, Wikidata, and the institution's own site (`farabi.university` is KazNU's own domain, not a separate institution); added a sourced `entity_aliases` row ("Al-Farabi University", Wikidata-attested), did not add unsourced "Farabi University". (3) Audited for further undetected duplicates (same-entity check, leading-"The", trailing-parenthetical, exact-normalized-name, shared-website_url, pg_trgm similarity >0.5 across all 1,019 rows) — found nothing beyond the known 9 pairs; the trigram pass's ~60 hits were all genuinely distinct institutions, none merged. Full detail: `docs/handoffs/duplicate-universities-audit-2026-08-22.md`. | `scripts/ingest-university-programs.ts`, `scripts/ingest-university-programs-batch.ts`, `scripts/ingest-requirements-deadlines.ts`, `scripts/ingest-university-requirements-batch.ts`, `scripts/stage-programs-ingestion-dryrun.ts`, `__tests__/programs/ingest.test.ts`, `docs/handoffs/duplicate-universities-audit-2026-08-22.md`. One live write: one `entity_aliases` row (sourced, no migration, no merge/deletion executed). | **Complete and pushed.** Gate clean: lint clean, typecheck clean, 110/110 test files, 1577/1577 tests (1574 baseline + 3 new). No migrations. No entity merges or row deletions — none were warranted. |

| FR-IT-REQUIREMENTS | Claude (same overnight session, pivoted to this lane 2026-08-21 after the programs-pipeline-reconciled/Turkey lane closed) | `oryn/fr-it-requirements-research` (isolated worktree at `.claude/worktrees/fr-it-requirements`, branched from `origin/main`@`e54c8a6`) | Admission-requirements and application-deadlines research for France (Sciences Po, Sorbonne Université, Université PSL, Université Paris Cité, Université Paris-Saclay) and Italy (Politecnico di Milano, Bocconi, Sapienza, Politecnico di Torino, Bologna, Padua) — the 11 institutions already covered in `fr_it_es_ch_batch1-6` programme catalogues. Follows the DE-NL-REQUIREMENTS lane's exact record contract and file-naming convention. **Complete: 61 records (39 requirements + 22 deadlines) across all 11.** Confirmed both of the brief's flagged non-scalar cases directly from institutions' own pages: Sciences Po's admissions pass mark is set annually by the jury after seeing that cycle's field (never published in advance, same shape as a concours); Sapienza/Politecnico di Torino's numerus-clausus quota only validates a non-EU applicant's pre-enrolment if they place among the winners of the official ranking — no numeric threshold anywhere in that fact. Also found a second non-scalar shape not in the original brief: Italy's OFA (obbligo formativo aggiuntivo) mechanism at Politecnico di Milano and Bologna, where the same CISIA test is a hard gate for restricted-access admission but only an OFA-assignment diagnostic for open-access programmes. 77% of deadlines found are `VERIFIED_HISTORICAL` (both countries' visible cycles had already closed by retrieval date 2026-08-21) — recorded as historical rather than dropped, matching the DE-NL lane's own precedent for closed-but-real dates. Full detail: `docs/research/university-requirements/fr-it-requirements-deadlines-summary.md`. | `data/research/university-requirements/fr_it_*.jsonl`, `docs/research/university-requirements/fr-it-requirements-deadlines-summary.md`. Research/evidence only, no schema/app/production/Supabase writes — same posture as DE-NL-REQUIREMENTS. | **Complete and pushed, idle pending next assignment.** All 19 files validated: zero duplicate IDs across the full set, all required fields present. Three institutions (Paris-Saclay, Politecnico di Milano, Bologna) have no deadlines file — each a disclosed, explained absence, not an oversight. |
| REQUIREMENTS-CONTRACT-DIGEST | Claude (new lane, 2026-08-22, assigned by the coordination session) | `oryn/requirements-contract-digest` (isolated worktree at `.claude/worktrees/requirements-contract-digest`, branched from `origin/main`@`b99ffe2`) | Docs-only digestion lane: folded the last ~48h of research-lane findings (extraction techniques, identity/evidence discipline, coordination-collision rules, deadline-modeling rules) into `docs/research/university-requirements/research-handoff-university-requirements.md`, the canonical research contract, each with a provenance citation (handoff doc, commit, or record ID) — see that file's "Techniques and rules earned since this contract was written" section. Checked `docs/ORYN-PROMPT-ARCHIVE.md` for currency: sections 4-7 (Counseling Intelligence, Global Opportunities Intelligence, Integration/Release Manager, Canonical Entity Intelligence) remain `PENDING`/`NOT COLLECTED` as the doc itself already discloses, and none of the requirements-research family (US/DE-NL/FR-IT-REQUIREMENTS, DE-NL-PROGRAMMES, UK-TR) has its own entry — flagged to the coordinator rather than fabricated, since this lane never received those sessions' original prompts and a guessed `VERBATIM` entry would violate the archive's own provenance rule. No code, no migrations, no schema, no Supabase writes. | `docs/research/university-requirements/research-handoff-university-requirements.md`, `docs/ORYN_WORKSTREAMS.md`, `docs/ORYN-PROMPT-ARCHIVE.md` (read-only check, no edits). | **Complete, gate green** (lint 0, typecheck 0, test 1,574/110 — matches baseline exactly), pushed. Idle pending next assignment. |

| TR-BILINGUAL-NAMES | Claude (new lane, assigned 2026-08-22 by the coordination session) | `oryn/tr-bilingual-programme-names` (isolated worktree at `.claude/worktrees/tr-bilingual-names`, branched from `origin/main`@`b99ffe2`) | **Package 1 (research, complete)**: 175/175 programmes researched, 174 paired to a sourced official Turkish name, 306 YÖK Atlas `kilavuzKodu` values captured (Bilkent 33/33, Boğaziçi 29/30, Gebze Teknik 23/23, Koç 22/22, Özyeğin 24/24, Yıldız Teknik 43/43), each sourced from the university's own bilingual pages, never translated-and-asserted. **Package 2 (wiring, complete)**: `matchYokPlacements` (`lib/programs/yok-atlas-matching.ts`) gained an optional `kilavuzBridge` param — an exact identifier match, checked before any name heuristic, defaulting to empty so all previously-working universities are unaffected — plus `lib/programs/tr-bilingual-name-bridge.ts` builds that map, wiring only `confidence: "high"` entries (fee tier rides inside the same record, so medium/low is a money-error risk, held back with a reason, never silently dropped — e.g. Özyeğin's "Computer Science"/"Bilgisayar Mühendisliği" case, real codes but the university's own site is internally inconsistent about the English name). 25 new tests, several against real sourced pairs (kilavuzKodu verified live before writing the test). **Dry run (not applied)**: 288/306 kilavuzKodu wired, predicting 288 new `university_program_placement_cycles` rows across the six with zero ambiguous groups and zero bridge-safety rejections — Bilkent 72, Boğaziçi 27, Koç 53, Özyeğin 59, Yıldız Teknik 53, Gebze Teknik 24; the 28 unmatched are all explained (unpaired-department sub-majors, genuinely absent departments, the one deliberate medium-confidence hold-back, one uncaptured language tier). Full detail: `docs/handoffs/tr-bilingual-programme-names.md`. | `data/research/university-programs/tr_bilingual_names_*.jsonl`, `lib/programs/tr-bilingual-name-bridge.ts`, `lib/programs/yok-atlas-matching.ts` (extended, not replaced), `scripts/ingest-yok-atlas-placements.ts` (extended), `docs/handoffs/tr-bilingual-programme-names.md`. | **Complete and pushed, idle pending next assignment.** Typecheck/lint/full test suite (111 files/1585 tests on this branch — behind the coordinator's stated 120/1,815 baseline because this branch hasn't merged the other lanes' recent work, not a lost-test regression) all clean. No DB writes, no migrations, no `--apply` run — insert stays pending explicit coordinator sign-off. |

| US-PROGRAMS-W2 | Claude (new lane, joined 2026-08-22, assigned by coordinator) | `oryn/us-programs-w2-research` (isolated worktree at `.claude/worktrees/us-programs-w2`, branched from `origin/main`@`b99ffe2`) | US programme-catalogue wave 2 — the zero-coverage universities. 106 US universities in the DB hold exactly 0 programmes; a sibling lane covers MIT/Stanford/Harvard/Yale/Princeton/Columbia/UC Berkeley/UCLA/Michigan/Georgia Tech/CMU/NYU, this lane covers 12 others — **complete: 1,173 records across all 12** (Brown 87, Dartmouth 53, Georgetown 74, Notre Dame 62, USC 202, UNC Chapel Hill 95, UVA 90, Vanderbilt 75, WashU St. Louis 84, Emory 89, Purdue 163, Rochester 99). Pre-dispatch DB check (read-only, 2026-08-22) confirmed all 12 exist as exactly one `universities` row each, all with `program_count = 0`, no duplicate rows. Dispatched as 6 parallel two-institution research agents. Georgetown/Notre Dame stalled twice — root cause was the agent nesting its own background sub-agents; resolved once told to do remaining work in the foreground directly, and that same nested sub-agent's independently-fetched evidence caught a real omission its own parent had made (see below). Full detail and process findings: this row + handoff note to follow. | `data/research/university-programs/us_programs_w2_*.jsonl`. Research/evidence only, no schema/app/production/Supabase writes. | **Complete and pushed 2026-08-22.** All 1,173 records validated: zero duplicate `research_program_id` and zero cross-file `official_program_url` collisions across all 12 files (checked globally, not just pairwise), every file's `university_name`/`university_country` a clean singleton matching its institution, zero MD/JD/DDS/PharmD/DVM leakage (Pharmaceutical Sciences BS and Veterinary Technology BS correctly kept as their genuine distinct bachelor's degrees). One post-commit correction: Notre Dame's Marketing major (Mendoza College of Business) was wrongly excluded in the first pass on the authority of a secondary advising page that was never actually fetched (blocked by a domain error), over a primary page a sub-agent had fetched and read — caught by the sub-agent's own conflicting report, verified directly, fixed in a follow-up commit (`06aa9ba`). |

| OPPORTUNITIES-THIN | Claude (new lane, joined 2026-08-22) | `oryn/opportunities-thin-2026-08-22` (isolated worktree at `.claude/worktrees/opportunities-thin-2026-08-22`, branched from `origin/main`@`b99ffe2`) | Opportunity discovery for the thin categories, assigned by the coordination session. Live-measured `opportunities` distribution 2026-08-22 (391 rows): `hackathon`=0 (valid enum, zero live rows — most severe gap), `conference`=2, `academic_program`=3, `fellowship`=5, `online_program`=6, `volunteering`=7, `student_program`=7, `entrepreneurship`=7, `internship`=8, `scholarship`=9, `research`=13, `competition`=72, `summer_program`=252. `eligible_countries` populated on only 26/391 (6.6%); prose-in-array bug (memory'd as fixed 2026-08-21) confirmed clean on this pass — zero rows matched a prose heuristic. `country` is split `Turkey`(8)/`Türkiye`(8) as two distinct literal strings — a normalization gap, not fixed by this lane (no DB writes). Starting research on the thinnest categories weighted toward Turkey/Europe per assignment; will write `data/research/opportunities/discovery_<category>_2026-08-22.jsonl`. | `data/research/opportunities/discovery_*.jsonl`. Research/evidence only, no schema/app/production/Supabase writes. | **Just started — measurement reported to coordinator, research in progress.** |

| OUTLOOK-EXPLAIN-SELECT-FIX | Claude (engineering lane, assigned by the coordination session 2026-08-22) | `oryn/outlook-explain-select-fix` (isolated worktree at `.claude/worktrees/outlook-explain-select-fix`, branched from `origin/main`@`b99ffe2`) | Two diagnosed-not-fixed defects, both fixed with content-asserting tests. **(1) University detail page "Gaps"/"Strengths" were a positional artifact, not a finding about the student**: `explainOutlook` (`lib/admissions/explain.ts`) took bare `{dimension: score}` numbers with no confidence, so an all-zero (unevidenced) profile — every dimension's scorer returns `confidence: "low"` with no underlying facts — still got sorted and sliced, always naming the same two dimensions (Career Exploration, Execution/Project Depth, since those sort last in `DIMENSION_SCORERS`) as "gaps" for every student with an empty profile. Fixed by threading confidence through: `explainOutlook` now takes `DimensionScoreInput[]` (`{dimension, score, confidence}`), excludes `confidence === "low"` dimensions from naming entirely, adds a deterministic score-then-alphabetical tie-break (so a genuine tie never resolves by array declaration order), and returns a new `insufficientData` flag the UI renders as "We don't know enough about this yet." instead of "None obvious yet." — Phase 68. Both callers updated: the university detail page's query now selects `confidence` too, and the (not-yet-wired-to-any-page) `counseling-adapter.ts`/`AdmissionOutlookSummary` carries the same flag through for whenever it is wired up. Strengths and Gaps deliberately got the same tie-break but not the same semantics — a Strengths tie among evidenced ≥55 dimensions is real incompleteness (not the single best two), never fabrication. **(2) Every `<Select>` in the app showed the raw stored value in its closed trigger** (a UUID, an enum like `not_started`) instead of the option's own label — confirmed systemic across all 13 files using `components/ui/select.tsx`, not just the "Start an application" modal that surfaced it. Root cause: Base UI's `<Select.Value>` only resolves a label from the declarative `items` prop on `<Select.Root>` (see `node_modules/@base-ui/react/select/root/SelectRoot.d.ts`) — it never reads a mounted `<Select.Item>`'s own children, unlike the Radix convention this codebase's existing `<SelectItem value={id}>{label}</SelectItem>` call sites were written against; the open popup rendered fine because that's a separate code path (`SelectItemText`). Fixed once at the shared component: `Select` now derives `items` automatically from its own JSX children (recursing through `SelectContent`/`SelectGroup`/etc. to find `SelectItem`s) unless the caller passes `items` explicitly, fixing all 13 existing call sites and any future one with zero call-site changes. | `lib/admissions/explain.ts`, `app/(app)/universities/[id]/page.tsx`, `lib/universities/counseling-adapter.ts`, `components/ui/select.tsx`, plus their tests. No DB writes, no migrations. | **Complete, gated, pushed.** `npm run lint` / `typecheck` / `test` / `build` all clean. Test suite grew from the 1,574/110 baseline to **1,587/112** (+8 in new `__tests__/admissions/explain.test.ts`: all-zero-profile names nothing, tie-break is order-independent for both strengths and gaps, low-confidence dimensions excluded even when others are evidenced, explicit `insufficientData` assertions; +5 in new `__tests__/ui/select.test.tsx`: closed-trigger text asserted directly for a UUID value, an enum value, a nested/grouped item, an explicit caller-supplied `items` override, and the no-selection placeholder case) — all content assertions, not mount-only checks. |

| PROGRAM-URL-REPAIR | Claude (new lane, assigned by coordinator 2026-08-22 after the original DE-NL-wave-2 brief turned out stale — live counts showed that work already done) | `oryn/university-program-url-repair-2026-08-22` (isolated worktree at `.claude/worktrees/url-repair`, branched from `origin/main`@`b99ffe2`) | Measuring and repairing `university_programs.official_program_url` quality across the whole table (9,423 rows / 132 universities, 0 nulls at the column level but the field is frequently misleading rather than absent). **Measurement complete, reported to coordinator, repair starting.** Categories found: shared/constant portal URL replicated across nearly all of a university's rows (2,815 rows / 33 universities — Manchester 294, Southampton 248, Wisconsin-Madison 217, TU Dublin 200, Bristol 62, others smaller); damaged/masked signal (420 rows / 3 universities — Durham 162 pagination links, Hamburg 197 and Universiti Sains Malaysia 61 both fully-distinct-URL-but-query-string-is-what-differs, collapsing to 1 base path each); actively misleading (St Andrews 87, points at a closed 2025-26 archive cycle presented as current — new category flagged by the UK programme lane); Turkish `yokatlas.yok.gov.tr` root (391 rows, excluded — another lane's identifier scope) plus 310 Turkish non-yokatlas shared-listing rows (Hacettepe/METU/ITU/Bilkent/Boğaziçi/Özyeğin/Koç/Sabancı/Eindhoven) held pending coordinator's call on scope. Two near-100%-distinct clusters confirmed legitimate, not defects: Göttingen's 21-row Master of Education shared hub page (previously verified by DE-NL-PROGRAMMES) and Sapienza's 4-row shared admissions-PDF cluster. UK lane (`uds:/tmp/cc-socks/81659.sock`) independently corroborated Durham/Bristol/St Andrews/Manchester numbers and handed remediation of those four to this lane (it owns building catalogues for Oxford/Cambridge/Imperial/UCL/LSE/Warwick/KCL, not repair). | `data/research/university-programs/url_repair_<institution>_2026-08-22.jsonl` — corrections only (programme identity + corrected URL + evidence), no schema/app/production/Supabase writes, no DB writes (read-only `execute_sql` for measurement). | **Measurement phase complete and reported. Repair phase starting**, prioritized by row-count impact: Manchester, Southampton, Wisconsin-Madison, TU Dublin, Hamburg (masking bug), Durham, USM, St Andrews (misleading), Bristol. Original DE/NL wave-2 assignment (14 institutions) confirmed already-clean live (matches DE-NL-PROGRAMMES's completed research) — no action needed there. |
| OPPORTUNITIES-VERIFICATION | Claude (new lane, joined 2026-08-22, assigned by the coordinator) | `oryn/opportunities-verification-2026-08-22` (isolated worktree at `.claude/worktrees/opportunities-verification`, branched from `origin/main`@`b99ffe2`) | **Phase 1 (verification, complete):** read-only live-source check of the 391-row `opportunities` catalogue — all 29 `active` rows with a past deadline, all 10 `source_confidence='high'` rows with a deadline inside 60 days, a 25-row random sample, and the 10 previously-fixed `eligible_countries` rows. Found and reported live: a marketing-tracking URL with an embedded personal email (fixed live by the coordinator within minutes), a scrape-artifact title, and a scrape-artifact source (a professor's CV page) — a 3-instance extraction-quality class. Zero fabricated opportunities anywhere. Full detail: `docs/research/verification/opportunities-verification-2026-08-22.md`. **Phase 2 (lifecycle fix, complete):** built `lib/opportunities/lifecycle.ts`, mirroring `NON_ACTIONABLE_VERIFICATION_STATES` (lib/deadlines/ingest.ts) deliberately — `NON_ACTIONABLE_OPPORTUNITY_CYCLE_STATUSES` (closed/historical/discontinued) plus a read-time `isOpportunityActionable` gate that also catches a passed deadline independent of cycle_status (self-healing, no reactivate step). `status` stays untouched (`active` still means "real record," distinct from `disabled`/`under_review`) — the fix reuses the already-existing, already-partially-populated `cycle_status` column instead of adding a new enum value. Wired into `persist-matches.ts`, the opportunities page's "For you" view, and the dashboard preview widget (defense in depth against stale `opportunity_matches` rows). Live measurement: 61 `active` rows already correctly labeled by research but ungated before this fix; 0 rows today need a cycle_status *data* correction (every past-deadline row was already labeled right); 100 null-deadline rows remain genuinely undetectable from stored data alone (documented honestly, not silently missed — needs a researcher or a future re-verification job, AGENTS.md Phase 30 Job B/E). Added `scripts/audit-opportunity-cycle-status.ts` (read-only) for ongoing maintenance. Gate: 111 files / 1599 tests (was 110/1574), lint+typecheck clean. | `docs/research/verification/opportunities-verification-2026-08-22.md`, `lib/opportunities/lifecycle.ts`, `__tests__/opportunities/lifecycle.test.ts`, `scripts/audit-opportunity-cycle-status.ts`, plus small read-path patches to `lib/opportunities/persist-matches.ts`, `app/(app)/opportunities/page.tsx`, `app/(app)/dashboard/page.tsx`. No database writes — all data corrections proposed to the coordinator as a batch, not applied by this lane. | **Complete, idle pending next assignment.** Proposed data corrections (DNA Day, Stanford ULO, Wharton FBW, Penn Medicine, LaunchX, Stanford SASI cycle_status) sent to the coordinator for review/application — none applied by this lane. Two `under_review` items (Wharton duplicate pair, Mathworks wrong-source row) flagged as fix-before-promotion, not yet actioned. |
| COUNSELOR-KNOWLEDGE | Claude (new lane, joined 2026-08-22, assigned by coordinator) | `oryn/counselor-knowledge` (isolated worktree at `.claude/worktrees/agent-ab2453fea600684bb`, fast-forwarded from a stalled prior attempt to `origin/main`@`2e061d6`) | Digested two days of admissions research (2,200+ requirement/deadline records, 13 countries, plus `docs/research/admissions-systems/`) into `docs/counselor-knowledge/<country>.md` — the distilled rules an AI counselor needs, not another data dump. Every claim labelled VERIFIED (corpus record id) or SYSTEM-LEVEL BACKGROUND (country-system research doc); disagreements between sources recorded, never picked. Prep for the AI Advisor (Phase 8) ahead of `ANTHROPIC_API_KEY` landing. **All 11 countries complete and pushed**: United States (ED/REA binding semantics, per-college test-optional variance, financial-aid variance, the year-offset labeling trap — 2 findings later corrected after a parallel conflicts lane closed them, `cc9ecd5`), United Kingdom (Oxbridge's earlier deadline/test/interview track, predicted-grade mechanic, TOEFL-rescale and test-provenance exclusions), Turkey (domestic YKS as algorithmic placement with zero holistic channel, TR-YÖS-only high-demand programmes), Germany (DSD≠academic-recognition — this package's highest-stakes finding, Studienkolleg gates, uni-assist's three deference relationships), Netherlands (Studielink's undated-statutory-deadline finding, per-university Turkish-diploma variance — corrected mid-pass, see below), France (Parcoursup-vs-DAP platform split, Sciences Po's after-the-fact pass mark), Italy (numerus clausus as ranking-against-quota, OFA's dual hard-gate/diagnostic role), Spain (sequential homologación→acreditación, floating nota de corte), Switzerland (four coexisting selection mechanisms, Turkish-applicant split), Ireland (CAO vs. non-EU/direct as two fully parallel systems), Canada (provincial not national centralization, McGill's four-way transcript evaluation). **Two docs (Germany, Netherlands) were corrected after a parallel conflicts-verification lane closed several originally-flagged conflicts** — most notably a wrong Groningen "older page is correct" claim (it was a 2×2 date table flattened into a list, not a real conflict; the actual older-page-correct case is Heidelberg's still-open uni-assist question), fixed same-day before wider consumption (`0a56e18`). Gate clean: lint/typecheck pass, 1574/110 tests unchanged from baseline (docs-only change). | `docs/counselor-knowledge/**`. Research/synthesis only — no schema/app/production/Supabase writes, no DB writes, no migrations. | **Complete, idle pending next assignment.** |

| PRODUCT-VERIFICATION | Claude (new lane, assigned 2026-08-22 by the coordination session) | `oryn/product-verification-qa` (isolated worktree at `.claude/worktrees/product-verification-qa`, branched from `origin/main`@`b99ffe2`, later merged forward to `origin/main`@`a315500` before the gate run) | Deep product/QA verification (Phases 43/45/46/61), then switched to fix mode by the coordinator for the two confirmed findings. **Constraint held throughout**: never completed a real signup or touched an inbox. **Dialog focus-trap — investigated, NOT fixed, and that's the right call**: traced the full causal chain in `node_modules/@base-ui/react` — `modal: true` correctly wired, `FocusGuard` spans render as designed, the guard's `onFocus` fires and computes the *correct* redirect target, but the actual `.focus()` call is deferred via a bare `requestAnimationFrame` in floating-ui-react's `enqueueFocus` that never ticks because this browser tool's tabs report `document.visibilityState: 'hidden'` even when "active" — the same root cause as the onboarding rAF-stall found earlier, now confirmed through a second, unrelated library (floating-ui-react vs. framer-motion). Coordinator accepted: `components/ui/dialog.tsx` deliberately left unchanged — downgraded from "confirmed app-wide bug" to "very likely a false positive from this session's own tooling," recorded here so it isn't rediscovered. **Fixed**: added `app/not-found.tsx` (root-level, mirrors `app/(app)/not-found.tsx`'s copy/shape, standalone since there's no sidebar at the root and the visitor may be unauthenticated) — routes outside the `(app)` group now get Oryn's branded 404 instead of Next's stock one. Branch was 107 commits behind `origin/main` at gate time (explains an initial 1,574/110 test-count mismatch against the 1,815/120 baseline) — merged `origin/main` forward (`0d41ed9`, clean, no conflicts) before re-gating. | Read-only investigation + one additive new file (`app/not-found.tsx`). No schema/production/Supabase writes, no DB writes performed, no migrations. | **Gate green post-merge: lint clean, typecheck clean, 120/120 test files passed, 1,815/1,815 tests passed — exact match to baseline. Pushed (`0d41ed9`). Report sent to coordinator, idle pending next assignment.** |

| UI-QUALITY-PASS | Claude (same session as PRODUCT-VERIFICATION, pivoted 2026-08-22 by the coordinator to a quality pass, not a defect hunt — AGENTS.md Phase 13/56) | `oryn/ui-quality-pass` (isolated worktree at `.claude/worktrees/ui-quality-pass`, branched from `origin/main`@`dc49a46`, merged forward twice before gating, latest `72ca9a1`) | **Headline finding**: `app/globals.css`'s `--font-sans: var(--font-sans);` was self-referential — a circular CSS custom property resolves to guaranteed-invalid, so `html { @apply font-sans }` silently fell through to the browser's Times serif default **app-wide** (confirmed via `getComputedStyle` — nav items, buttons, all body copy were literally `font-family: Times`, everywhere except `font-heading`-tagged elements). Invisible to a pixel-level defect hunt; found via computed styles, not screenshots. Fixed to `var(--font-geist-sans)`, matching the correct pattern the adjacent `--font-mono`/`--font-heading` lines already used. **This invalidates the prior UI sweep's Phase 13 verdict** (the app was assessed while rendering in the wrong typeface) — redone from scratch post-fix, see below. Also: added `app/(app)/dashboard/loading.tsx` (only page-group missing one; dedicated skeleton mirroring the page's real block layout, since it's the one page the spec gives an exact reference layout for) + a `DashboardSkeleton` export in `components/oryn/loading-skeleton.tsx`; added `generateMetadata` to 5 dynamic-route pages that shared the generic layout title or a static placeholder (`universities/[id]`, `opportunities/[id]`, `applications/[id]`, `u/[id]`, `messages/[userId]`) — the two social pages replicate the page body's own privacy/visibility gating exactly rather than a wider query, so a private profile's name can't leak into a tab title where the page itself would 404 for that viewer; normalized `settings`/`documents`/`admin` page headings (raw `<h1>` missing `font-heading` entirely, rendering in the wrong typeface, plus wrong font-weight) onto the shared `PageHeader` component. Audited interaction states (Button/Input primitives, inline text-link convention) and empty-state coverage (Advisor/Portfolio/Story-bank chains) — both already solid app-wide, no changes needed, reported as a real finding rather than invented gaps. **Phase 13 redo (post-fix)**: calm, generous whitespace, one accent system, restrained color, no gamification/giant grids/childish illustration/unnecessary animation — holds up well across Dashboard/Universities/Opportunities/Profile/Documents/Settings/Plan at desktop and mobile now that the correct typeface renders; verdict stated explicitly as superseding the pre-fix assessment, not disagreeing with it. | UI/copy/CSS only. No schema/production/Supabase writes, no migrations, no destructive actions. | **Gate green: lint clean, typecheck clean, 121/121 test files, 1,826/1,826 tests (exceeds the 1,815/120 baseline — new tests landed on `main` from other lanes during this pass, none dropped). Pushed (`72ca9a1`). Report sent to coordinator, idle pending next assignment.** |
| SCHEMA-GAPS-DESIGN | Claude (new lane, joined 2026-08-22, assigned by coordinator) | `oryn/schema-gaps-design` (isolated worktree at `.claude/worktrees/schema-gaps-design`, branched from `origin/main`@`2601c71`, merged forward to `origin/main`@`90f70f7`) | Consolidated the night's individually-flagged "the schema can't hold this fact" findings into `docs/handoffs/schema-gaps-design-2026-08-22.md`, four buckets (structural eligibility that isn't a threshold; same name different programme; an instrument whose nature varies; sources stale but authoritative + a new "un-refreshable" state), migration `0059` (unapplied). **Reconciled and complete**: first pass could not confirm 5 of 11 briefed findings (Ghent, Brookes Engage, Vienna, NTNU, Manchester's specific framing) against a corpus snapshot at this branch's own start point — all landed on `main` afterward in a Nordic/Benelux batch and an opportunities-discovery batch; re-verified every one directly with exact record IDs after merging `origin/main` forward. None of the corrections changed the migration's SQL (Ghent/Brookes Engage/NYU Nursing all independently confirmed conclusions already reached without them). One genuinely new item folded in from a peer lane's same-day finding (`docs/handoffs/yok-placement-key-gap-2026-08-22.md`): 23 real Turkish programmes carry two genuine admission tracks colliding on one `university_program_placement_cycles` key — added as migration item B6, widening that table's unique index to include the already-stored `kilavuz_kodu`, with the deeper "widen the index vs. split the programme row" question left as an explicit judgement call (recommends widening now, reversible, revisit splitting once population size is measured). **Six forced schema changes total**, all additive. Gate clean at 121/1826 (post-merge baseline). **Follow-up measurement complete**: reused the already-tested `matchYokPlacements()`/`buildKilavuzBridge()` against the already-fetched YÖK snapshot + a read-only `university_programs` pull to measure the real population B6's judgement call needed. Distribution across all 779 TR programmes: 171 with 0 attributed kilavuzKodu, 523 with 1, 85 (10.9%) with 2+. Of those 85, 67 (79%) are scholarship-tier variants already safe under the existing key — B6 not even needed. Only 18 (2.3% of 779) genuinely collide without B6, concentrated at 2 state technical universities (Yıldız Teknik, Gebze Teknik), split 8 language-of-instruction / 6 KKTC-citizenship-quota / 4 both — zero day/evening cases, disconfirming the original hypothesis's second guess. Reported the split (fee-tier=safe, KKTC-quota=index-widening sufficient, language-split=arguably a missing `university_programs` row) without picking a side, per instruction to report the distribution before any recommendation. Full detail: `docs/handoffs/yok-kilavuz-distribution-measurement-2026-08-22.md`. | `docs/handoffs/schema-gaps-design-2026-08-22.md`, `supabase/migrations/0059_schema_gaps_2026-08-22.sql` (unapplied), `types/database.ts`, `docs/handoffs/yok-kilavuz-distribution-measurement-2026-08-22.md`, `scripts/measure-yok-kilavuz-distribution.ts`, `data/research/tr-university-programs-2026-08-22.json`, plus fixture/test updates. No DB writes, no migration applied. | **Complete, idle pending next assignment.** B6 reviewed and settled by the coordinator: widen the index (18/779 genuine collisions is not evidence of a modelling error). The language-of-instruction subset (12 rows) is recorded as a known catalogue inconsistency, not migrated on — too thin a sample today, flagged for future Turkish expansion. Five product-policy judgement calls remain open for the founder (down from six). |
| UK-PROGRAMMES | Claude (new lane, 2026-08-22) | `oryn/uk-programme-catalogue` (isolated worktree at `.claude/worktrees/uk-programmes`, branched from `origin/main`@`b99ffe2`) | Assigned by the coordinator to build real undergraduate catalogues for 7 near-empty flagship UK universities: Oxford, Cambridge, Imperial, UCL, LSE, Warwick, KCL. Live-measured all 12 originally-named institutions first (found the brief's "UCL 7, LSE 5, most others fewer or zero" assumption was stale — Edinburgh 95/Bristol 62/St Andrews 87/Durham 162/Manchester 294 already had rows, but 4 of those 5 turned out to have broken-provenance URLs, handed off to a separate URL-provenance lane per the coordinator rather than worked here). **All 7 complete: 934 new records.** Oxford 4→52 (48 new + a same-day degree-type follow-up pass fetching all 48 course detail pages, reclassifying 13 integrated-master's courses e.g. Chemistry→MChem, Physics→"MPhys / BA"). Cambridge 7→33 (26 new; caught a stale existing-row URL for Law). Imperial 6→73 (67 new; caught 2 more stale existing-row URLs). UCL 7→429ish (422 new via direct DOM extraction across 22 pages since the accessibility tree didn't scale — SSEES bilingual-language-pair matrix recorded individually per coordinator confirmation; disclosed a 1-course gap against UCL's stated 434, likely live-pagination drift; caught 2 more stale existing-row URLs). LSE 5→43 (38 new; caught that LSE's search tool defaults to 257 results across ALL study types mixed together, not just undergraduate — filtered explicitly). Warwick 4→190 (186 new; caught two exact-duplicate cards in Warwick's own listing, deduped by URL). KCL 5→152 (147 new; iBSc intercalated-year and graduate-entry routes classified distinctly). Every institution's real per-course URLs read from the site's own rendered listing/DOM, never slug-guessed — this discipline caught 7 total stale-URL cases across 4 institutions (Cambridge×1, Imperial×2, UCL×2, KCL confirmed clean) that were pre-existing defects in the live DB, not introduced here. | `data/research/university-programs/uk_programs_*.jsonl`. Research/evidence only, no schema/app/production/Supabase writes — same posture as DE-NL-PROGRAMMES. | **Complete and pushed.** All 7 institutions done (Oxford, Cambridge, Imperial, UCL, LSE, Warwick, KCL), 934 new programme records, idle pending next assignment. Stale-URL findings (7 cases across Cambridge/Imperial/UCL) flagged to the coordinator's separate URL-provenance lane rather than fixed here (out of scope for a research-only lane). |

| CA-PROGRAMMES | Claude (same overnight session, pivoted to this lane 2026-08-21 late/2026-08-22 after the QA-fixes and Important-dates packages closed) | `oryn/ca-programmes` (isolated worktree at `.claude/worktrees/ca-programmes`, branched fresh from `origin/main`; the prior `oryn/canada-programme-catalogue` branch was checked first and discarded — zero unique commits) | Undergraduate/graduate programme-catalogue research for 8 named Canadian universities, assigned by the coordinator after confirming the live baseline (Waterloo 105 records; all other 27 Canadian universities 0). **Complete: 3,651 records across all 8** — Toronto 635, UBC 546, McGill 288, Montréal 679, Queen's 337, Alberta 179, McMaster 432, Western 555. Central finding: co-op status is a per-programme fact with no reliable institutional default — UBC uses a same-record Yes/No flag exclusively (confirmed across all 196 of its programmes), Alberta splits co-op into fully separate programme codes even at the sub-option level, and both Toronto and McMaster use *both* mechanisms internally depending on the specific programme, disproving any single per-institution convention. Queen's separately inverts a related assumption: its Professional-Internship-integrated plan codes (`-I` suffix) are the closed ones while the matching base plan codes remain open, confirmed via the calendar's own footnote on 4 separate Computing plans. Found a real regional posture absent from the entire prior DE/NL/UK/priority-country corpus: 3 of the 8 institutions (McGill, McMaster, Western) block AI crawlers by name in robots.txt, versus zero occurrences before this lane — in every case a permitted alternative host or citation path existed and was used instead of routing around the block. 3 of 8 research dispatches (Alberta, McMaster, Western) stalled with zero output on first attempt ("no progress for 600s"), matching a session-wide pattern of 8 total stalls diagnosed elsewhere as nested sub-agent spawning; fixed on every retry by re-dispatching fresh with an explicit "write incrementally, don't block on one slow step" instruction. Two sourcing judgment calls (McGill's one-cycle-old Wayback sourcing under a WAF gate; Western's citation of a blocked host's URL as published by an unrestricted feed) were flagged to and confirmed by the coordinator rather than decided unilaterally. Full detail: `docs/research/university-programs-ca/README.md`. | `data/research/university-programs/ca_programs_*.jsonl`, `docs/research/university-programs-ca/**`. Research/evidence only, no schema/app/production/Supabase writes — same posture as the other programme-catalogue research rows. | **Complete and pushed, lane stood down per explicit coordinator instruction ("I'll handle the ingestion").** All 8 files schema-validated and deduplicated by `research_program_id` before commit — zero mismatches, zero duplicate IDs. Not yet ingested into the live DB; that step is owned by the coordinator, not this lane. |
| CANONICAL-LIVE-COLUMN | Claude (new lane, assigned 2026-08-22 by the coordination session) | `oryn/canonical-live-column-refactor` (isolated worktree at `.claude/worktrees/canonical-live-column`, branched from `origin/main`@`dcce22f`; a first attempt at this lane stalled for 600s during read-only investigation with zero commits and was auto-cleaned — this is the recreated worktree, resumed from the original brief with a commit-early-and-often discipline this time) | **Complete.** Re-verified the DUPLICATE-UNIVERSITIES row's own closing note directly against `oryn-qa-scratch` before starting: `duplicate_status` is canonical=1010/superseded=9, and all 9 superseded ids' `superseded_by_id` exactly match `lib/universities/duplicate-supersessions.json`'s 9 entries (spot-checked every id, not just the count) — re-checked again after finishing, unchanged. `lib/universities/canonical.ts`'s own header already correctly named this as the one remaining step — DDL and backfill done, only the application-layer JSON read path was never switched over. Grepped the current real consumer set fresh rather than trusting the brief's "~30 files" estimate: **23 files actually import from the module** (not 30) — 6 under `app/(app)/{applications,universities}/**`, 9 under `lib/**`, and 8 under `scripts/**`. The brief's "~14 scripts, mostly the acquire-university-statistics-* family" was the one genuinely wrong part of the estimate: those 11 statistics scripts never import the module at all — they only mention the JSON filename in a prose comment (a one-time manual cross-check note from when they were hand-authored); given comment-only touch-ups anyway since the file they reference is now deleted. The real scripts/ consumer set is the ingestion/dedup pipeline family (`ingest-university-programs.ts` + 3 siblings, `acquire-university-images.ts`, `university-image-coverage-report.ts`, `stage-programs-ingestion-dryrun.ts`) plus the now-retired generation script itself. All 23 consumers switched to an async `loadSupersessionMap(supabase)`/`loadSupersessionMapViaRest(target)` + 4 pure map-consuming helpers, one exception (`stage-programs-ingestion-dryrun.ts` has no live DB connection at all — built its map locally from an already-loaded JSON snapshot, extended with the two new columns). Two real bugs found by typecheck, not anticipated in the brief: `types/database.ts` (hand-authored, not regenerated) never had `duplicate_status`/`superseded_by_id` added when migration 0043 shipped — added both; `lib/search/index.ts`'s `searchApplications()` had a second `canonicalUniversityId` call site the first lib/ pass missed. Full detail: `docs/handoffs/canonical-live-column-refactor-2026-08-22.md`. | `lib/universities/canonical.ts` (async DB-backed rewrite — `loadSupersessionMap`/`loadSupersessionMapViaRest` replace the static JSON import; `pickCanonicalWinner`/`DuplicateCandidate` kept, out of scope per the assignment), `lib/universities/duplicate-supersessions.json` (deleted), `scripts/resolve-university-duplicates.ts` (deleted — its only output was the now-deleted JSON file), the 23 real consumer files, their test files (`__tests__/universities/canonical.test.ts`, `__tests__/universities/duplicate-regression.test.ts`, `__tests__/entities/search.test.ts`, `__tests__/programs/ingest.test.ts`), `types/database.ts`, `lib/dev/fixtures.ts`, `package.json` (removes the `resolve:university-duplicates` script entry), and a light comment-text-only touch-up on the 11 `acquire-university-statistics-*.ts` files. No entity-merge/identity logic touched — read side only, per the assignment's explicit scope. | **Complete and pushed.** Gate clean: lint clean, typecheck clean, 121 files/1824 tests passed (baseline 121/1826 — the −2 is two tests whose entire premise was diffing against the now-deleted JSON snapshot, both removed with an explanatory comment, not silently dropped), `npm run build` succeeds (all 39 routes). Not merged to `main` — needs review. |

| OPPORTUNITIES-ELIGIBLE-COUNTRIES | Claude (new lane, joined 2026-08-22) | `oryn/opportunities-eligible-countries-gap` (isolated worktree at `.claude/worktrees/opportunities-eligible-countries`, branched from `origin/main`@`dcce22f`) | Assigned to close `opportunities.eligible_countries` — 366/391 (93.6%) null/empty, re-verified independently against a live count matching `docs/current-state.md`'s snapshot exactly, the single gate blocking real eligibility matching per `docs/MASTER-EXECUTION-STRATEGY.md` §P3. Read `lib/opportunities/matching.ts`/`lib/counselor/eligibility.ts` directly before writing anything: confirmed empty `eligible_countries` already means "not restricted by country" in both the hard-filter and counselor layers — so a genuinely open program must stay empty (populating it would be wrong, not just redundant), and the real live risk is the inverse: a genuinely *restricted* program with an empty array is shown as eligible to every student today, with no visible warning. **Step 1 (deterministic backfill from data already on the row, applied): 13/366 rows.** Bucketed all 366 by signal type; only rows where `citizenship_restrictions`/`residency_restrictions` already held an explicit, unambiguous single-country statement qualified — title/organization keyword-matching (25 candidates) and description-keyword scanning (8 candidates) produced **zero** safe applies, a genuine negative finding recorded, not a shortfall. Confirmed the "Türkiye Scholarships" trap named in the assignment brief is real and handled correctly: its own `citizenship_restrictions` text reads "Open to citizens of all countries" — the opposite of what a naive title-pattern match would suggest — left untouched. **Step 2 (real research batch, applied): 24 programs researched to official-source-first standard, 1 populated** (UWC Türkiye — compound citizenship/parent-citizenship/5-year-residency rule, sourced via WebSearch-indexed excerpts since direct `tr.uwc.org` fetch 403'd twice), 5 confirmed explicitly open (incl. a newly-found duplicate pair, "Diamond Challenge"/"The Diamond Challenge"), 1 genuine conflict recorded not resolved (HOSA's two disagreeing international-chapter lists), 16 checked against a real official page with no restriction found (incl. a counter-finding against the obvious prior: an official `legion.org` article confirms Ohio's American Legion Boys State explicitly welcomes non-citizens), 2 well-scoped-but-incomplete (TechGirls' exact 37-country list, Erasmus+'s Programme/Partner-country structure — both stay null deliberately per RULE-ELIGIBILITY-009, not for lack of evidence). **Net: 366 → 352 (90.0%), +14 populated.** Full detail, per-row reasoning, and a prioritized scoped plan for the remaining 352: `docs/research/opportunities-eligible-countries/README.md`. | `data/research/opportunities/eligible_countries_step1_2026-08-22_dry-run-update.sql`, `data/research/opportunities/eligible_countries_step2_2026-08-22_batch1.sql`, `docs/research/opportunities-eligible-countries/**`. One live write scope: `opportunities.eligible_countries` (+ `citizenship_restrictions`/`source_confidence` only on the 1 row those directly support). No schema/migrations/app code touched. | **Complete and pushed for this pass, idle pending next assignment.** Both SQL files idempotent by construction (every `UPDATE` guarded by `id = X AND (eligible_countries IS NULL OR eligible_countries = '{}')` — safe to re-run as a no-op). Incidental findings flagged to whoever owns general opportunities data quality: 3 garbled/multi-program scrape rows (Robomaster China, a Netherlands-titled row with Italy content, York Helix), the Diamond Challenge duplicate pair, and İTÜ's current 2026 session dates (found incidentally, not applied — outside this lane's `eligible_countries` scope). |

| RES-R2 | Claude (research-org lane per `docs/ORYN-ORG-STRUCTURE.md`, claimed 2026-08-22, coordinator-managed background agent reporting to ORYN-CEO) | `oryn/res-r2-opportunity-deadlines` (isolated worktree at `.claude/worktrees/agent-aaee44f0d87e06c58`, branched from `origin/main`@`38d7be7`) | Package 1: current-cycle application deadlines + cycle status for live `verified_current` opportunities, per the RES-R2 brief in `docs/ORYN-ORG-BRIEFS.md`. Re-measured live 2026-08-22 before starting: `verification_state='verified_current'` non-summer rows = 101 total; this package's five scope categories = **74 rows** (competition 47/15 dated, scholarship 8/5, research 7/1, internship 7/1, fellowship 5/0). Output: JSONL research records in `data/research/opportunities/dlopp_batch*.jsonl` (~15 rows/batch, ID prefix `DLOPP-`), each carrying opportunity id, found deadline or explicit none-published finding, cycle-status evidence, source URL, verbatim source language, retrieval date, confidence + reason. Evidence rules per brief: undated recurring deadlines recorded verbatim with no synthesized year; closed cycles recorded as historical facts; enrollment-year vs cycle-year page-labeling checked per page; conflicts recorded never resolved; robots.txt AI-crawler blocks respected (deferred-with-reason if no permitted official alternative). | `data/research/opportunities/dlopp_batch1-5.jsonl` (74 records), `docs/research/opportunities-deadlines/README.md` (contract + outcome summary), `docs/handoffs/res-r2-opportunity-deadlines-2026-08-22.md`, this row. Research/evidence only — no schema/app/production writes; Supabase MCP used read-only. Live-DB writes belong to RES-I2 after RES-V1/V2 verification. | **Package 1 COMPLETE and pushed (2026-08-22).** 74/74 scope rows covered, one record each, all UUIDs re-verified live (74/74 exact match). Outcomes: 14 dated current-cycle (7 open + actionable now, incl. Gates 09-15/Coca-Cola 09-30/QuestBridge 10-01), 8 undated-recurring recorded verbatim with no year ever synthesized (incl. Türkiye Scholarships' year-less national calendar), 21 closed-historical dated facts, 26 verified-absences (incl. by-design rolling journals and structural no-central-deadline models), 5 deferred with reason (2 robots.txt AI-crawler blocks respected — Technovation, CSHL; 3 server-side 403s — BSPEE, Ashoka, Girl Up). 5 conflicts recorded never resolved (Conrad off-by-one vs stored value; IPsyO page-cycle regression; IPPF same-site season-label split; CEMC stored-provenance gap; Özyeğin stale-open banner). Notable deltas for verifier/ingester: SIP-UCSC stored cycle_status now provably stale, Ron Brown's stored dated deadline is projection-only, We the People application_url 404s, UK Chemistry Olympiad registration close (2027-01-11) is a new dated fact. Idle pending next package (summer_program subset, 87 rows). |
| RES-V1 | Claude (research-org verifier lane per `docs/ORYN-ORG-STRUCTURE.md`, claimed 2026-08-22, reports to ORYN-BASORG) | `oryn/res-v1-validation` (isolated worktree at `.claude/worktrees/res-v1-validation`, branched from `origin/main`, fast-forwarded to `ce3ac74` before finalizing) | Contract/schema/ID validation of RES-R2's package-1 DLOPP batch (`dlopp_batch1-5.jsonl`, 74 records) per the RES-V1 brief in `docs/ORYN-ORG-BRIEFS.md`: parse/required-field/keyset checks, ID uniqueness within-batch + corpus-wide (exact-ID-string collision, scoped per BASORG's RULE-CORPUS-ID-001 note — not a blanket global-uniqueness assertion, which would false-positive on the program corpus's ~536 intentional revision-pair IDs), live identity reconciliation (74/74 `opportunity_id`s exist, title/category/verification_state/status all exact), and a live-schema enum-vocabulary check (`cycle_status_found` against `opportunities.cycle_status`'s actual CHECK-constraint vocabulary, not the research contract's copy of it). That last check surfaced a real gap — 9 records use `cycle_status_found="unknown"`, which has no live equivalent — and a follow-up monotonicity audit (built at ORYN-BASORG's request after the first finding, then expanded from 9 records to all 74 × 3 candidate write-target fields per BASORG's second request) found 21 total cases where a naive field-by-field ingestion write would replace real, informative live data with something less informative, 6 of them live-destructive right now (would strip a determined `cycle_status`, including "open" on Ashoka Young Changemakers, down to "unverified"). Ratified by BASORG as standing rule RULE-INGEST-003 (information-monotonic writes, all columns) mid-verification. Built the reusable validator (`scripts/validate-research-records.ts`) the brief calls for as part of this pass, not separately — found and fixed 2 real bugs in the tool itself against this exact batch (a bare-`origin`-ref filter gap, and a same-file exclusion that broke when the batch merged to `main` mid-verification via PR #13 — fixed by deduping on git blob hash instead of path/branch strings) before trusting its output. | `scripts/validate-research-records.ts` (new — generic 4-pass engine + a `dlopp` lane contract; adding the next lane is one config object, not new engine code), `docs/research/verification/v1_dlopp_verdict.md` (verdict + full monotonicity evidence tables), this row. Verifier posture: never edited RES-R2's files, never wrote to the live DB — every check is a read-only Supabase MCP query or REST read. | **DLOPP package complete, merged (PR #20). Verdict: PASS on research/contract quality (0 contract defects, 0 ID collisions, 74/74 live-identity matches) — ingestion was EXPLICITLY GATED** on the RULE-INGEST-003 monotonicity guard (the verdict's §5 tables were the concrete test set); RES-I2 built the guard off that table directly. Also flagged, not fixed (outside this lane's scope): `docs/ORYN-ORG-STRUCTURE.md`/`ORYN-ORG-BRIEFS.md` are not on `main` (only on lane branches). **Package V1-2 (assigned by BASORG after the DLOPP verdict): dedup-key convention-drift audit, generalizing the Glasgow near-miss** (RES-I1 dry-ran `acquire-programs-batch2`, got 101 net-new/0 duplicate for Glasgow — actually 69 duplicates, hidden by two independent naming-convention drifts the composite `programDedupKey` can't see through: live rows carry code-stripped names + null `degree_type`, the research file carries code-suffixed names + populated `degree_type`). Built `scripts/audit-dedup-convention-drift.ts`, reusing the real unmodified `resolveUniversity`/`programDedupKey`/`normalizeProgramName` (not a reimplementation), calibrated against Glasgow's exact known 69/101 split before trusting it corpus-wide — found and fixed 3 real bugs in the tool during calibration (nested-bracket stripping, over-aggressive multi-group stripping that ate a real qualifier like "(Primary)", and a same-URL last-write-wins collision that produced false leads on Radboud's 49-programme shared catalogue-listing URL before the fix required an exact name match whenever a URL is shared by more than one live row). **Result: ran the reusable tool against the ENTIRE not-yet-ingested corpus (19,657 records, 131 files, 0 identity-resolution failures) — the blind spot currently exists in exactly one place, Glasgow's batch2 (68 high + 1 medium confidence, 69 total), fully reconciled against every other "strict-key new" record in the corpus.** Report-only per the assignment's hard constraints: no dedup-key changes proposed, no merges performed, every finding is a review-queue candidate. | `scripts/audit-dedup-convention-drift.ts` (new), `docs/research/verification/v1-2_dedup-convention-drift-audit.md` (full report — calibration detail, corpus reconciliation table, review queue, and a section on convention drift as a general disease per BASORG's `field_provenance`/`international_eligible` framing), `package.json` (adds `audit:dedup-convention-drift` + `validate:research` npm script entries), this row. Read-only throughout — no live writes, no researcher/pipeline files touched. | **Complete, gate green (typecheck clean, lint clean, 122 files/1861 tests pass), pushed, PR open. Idle pending BASORG's next assignment.** Recommendation for BASORG (not decided here): the 68 high-confidence Glasgow pairs are strong candidates to simply not ingest from `acquire-programs-batch2`; the 1 medium pair + Glasgow's other 32 URL-unmatched records need research/founder-level review. Also worth folding into the same discipline as RULE-DEDUP-001 (opportunities): a report-only tool nobody re-runs is operationally indistinguishable from no tool. **Package V1-3 (assigned by BASORG): first-ever V-pass on RES-R3's `ECW3`/`ECW4` eligible_countries waves** (`ecw3_batch1-3.jsonl`, 55/55 complete `competition`-category records; `ecw4_batch1.jsonl`, 22/~213 `summer_program`-category records in progress) — nothing had verified R3's output since wave 2. Extended `scripts/validate-research-records.ts` with a second lane (`ecw`), proving the one-config-object reusability claim across a structurally different contract (16 fields, no `db_state_at_research` baseline unlike DLOPP, a citizenship-restrictions prose-write path DLOPP never had). Checked monotonicity in BOTH directions this time per BASORG's explicit instruction (DLOPP's verdict had only covered erasure; RES-I2's guard separately found replacement cases it had missed) — came back clean not because the check was narrow but because `proposed_eligible_countries` is null on all 77 records (zero populate proposals in either wave) and the one actual write proposal (ECW3-016, a `citizenship_restrictions` prose addition) targets a confirmed-null live field. Spot-checked BASORG's named research distinctions against full record content, not just the `finding` label — the "defined eligible category" (Duke/Parsons, confirmed-open) vs. "resources page" (Cornell/Georgetown/Harvard SSP, no-statement) split holds consistently across all 5 checked records, and RULE-ATTRIBUTION-001 is correctly applied on both named multi-product-page cases (Iowa Young Writers', Johns Hopkins CTY — both correctly declined to credit a different track's language to the researched row). One title-field finding explained not blocking (ECW3-004 carries a researcher-added `[Turkiye chapter row]` annotation flagging a likely duplicate-pair with the already-confirmed-open global FIRST Robotics row — correct scope discipline, not touched). One process observation flagged, not ruled on: 15 of 55 ECW3 records (27%) reference opportunities whose live `status` is `under_review` (all from one 2026-08-18 bulk-discovery batch, not yet through the admin quality gate) — whether ECW's scope should exclude not-yet-vetted rows is a scope question this audit surfaces, not decides. Added an explicit §7 scope statement (covered/not-covered failure classes) from the start this time, per the standing rule the DLOPP verdict's silent erasure-only gap prompted. | `scripts/validate-research-records.ts` (extended — `ecw` lane added alongside `dlopp`), `docs/research/verification/v1-3_ecw3_ecw4_verdict.md` (full verdict), this row. Read-only throughout — no researcher files edited, no live DB writes. | **Complete, gate green (typecheck clean, lint clean, 124 files/1888 tests pass), pushed, PR open. Idle pending BASORG's next assignment.** Verdict: PASS (77/77 contract + ID + live-identity, 0 monotonicity findings either direction). Not this lane's call: whether the `under_review` rows should have been in scope, and ECW3-004's duplicate-pair lead — both routed to BASORG. |
| GATE-FIX | Claude (code lane, assigned 2026-08-22 by ORYN-CEO, founder-approved this hour) | `oryn/evidence-gate-retrieval-method` (isolated worktree, branched from `origin/main`@`38d7be7`) | **Complete.** Implemented `docs/handoffs/evidence-gate-false-rejections-2026-08-22.md`'s founder-approved option 2: the evidence gate now routes on a structured `retrieval_method` closed enum (`live_fetch`/`browser_render` pass, `archived_capture`/`search_summary` fail) declared on the research record contract, one shared implementation (`lib/acquisition/retrieval-method.ts`) for both the programs and opportunities pipelines. Backward-compatibility rule: records without the field are prose-matched exactly as before (bar never drops); malformed values fail closed; a contradiction guard fails any passing method whose own prose discloses a blocked/archived retrieval. Evidence-derived backfill of the blocked CA corpus (deterministic script + committed output, per-line round-trip guard, no hand-edits): 2,094 of the 2,097 wrongly-blocked records classified passing, 3 Western records held back on their own attestations' qualifications (One Health 404 pair + IMS not-re-fetched single), McGill's 288 classified `archived_capture` and confirmed still blocked. Dry run (NO ingestion): Montréal 679, Queen's 337, Alberta 96, Western 545 accepted end-to-end = **1,657 ready for a separate ingestion package**; McMaster's 432 + Western/Huron's 5 now surface the SEPARATE domain-authority defect (registrar-contracted `romcmaster.ca` / affiliated-college `huronu.ca` hosts fail `sourceAuthority` — the Dartmouth-class problem, deliberately NOT widened here, escalated to CEO). Notable finding: Alberta's prose-gate outcomes were inverted (its 83 ingested records are the index-derived ones, its 96 blocked ones the per-page-fetched ones). No migration needed — verified against the live migration list (ends at 0056), not assumed. Full detail: `docs/handoffs/gate-fix-retrieval-method-2026-08-22.md`. | `lib/acquisition/retrieval-method.ts` (new), `lib/programs/ingest.ts`, `lib/opportunities/ingest.ts`, `docs/research-handoff-university-programs.md`, `docs/research-handoff-opportunities.md`, `scripts/backfill-retrieval-method.ts` (new), `data/research/university-programs/ca_programs_{alberta,mcgill,mcmaster,montreal,queens,western}_2026-08-22.jsonl` (+ backfill report JSON), `__tests__/acquisition/retrieval-method.test.ts` (new), `__tests__/programs/ingest.test.ts`, `__tests__/opportunities/ingest.test.ts`, `docs/handoffs/gate-fix-retrieval-method-2026-08-22.md`. No DB writes, no `--apply` runs, no migrations. | **Complete, gate green (lint/typecheck clean, 122 files / 1,850 tests, build succeeds), pushed, PR open — awaiting CEO verification. Ingestion of the 1,657 explicitly NOT done here.** |
| FEAT-2 | Claude (org lane per `docs/ORYN-ORG-BRIEFS.md` §FEAT-2, claimed 2026-08-22) | `oryn/feat2-loop-audit` (isolated worktree at `.claude/worktrees/feat2-loop-audit`, branched from `origin/main`@`85c3d65`, re-fetched mid-package to `ce3ac74` — diff confirmed zero territory-code files changed) | **Package 1 — half-done inventory of FEAT-2's territory** (weekly plan/actions, reflection loop, application tracker, deadline engine, notifications, monthly review, goals, time budget/busy mode). Audited every feature against claimed/code/browser-verified status. Real findings, most-to-least severe: (1) **escalated to CEO mid-package, ruled Package 2, ahead of this ranking** — `lib/deadlines/upcoming.ts`/`scan.ts` don't filter opportunities by `cycle_status`, so a closed-cycle-but-future-dated opportunity (confirmed live: LaunchX, `cycle_status='closed'`, `status='active'`, deadline 82 days out) can render as "Due soon" and trigger a deadline-reminder notification — the dashboard's own opportunity-match preview already guards this via `lib/opportunities/lifecycle.ts`'s `isOpportunityActionable`, the deadline engine just never calls it; (2) `/plan` page's "Generate my plan" button (`features/dashboard/generate-plan-button.tsx`) silently swallows every error — reproduced live with `ANTHROPIC_API_KEY` genuinely unset: click produces zero feedback, no toast, no message, unlike the dashboard's own EmptyState three files away which handles the identical failure correctly; (3) `weekly_actions.reflection_note` (free-text) has no UI input anywhere despite the DB column, the AI context, and the action's own parameter all supporting it; (4) `skipped`/`expired` action statuses are permanently unreachable — no UI control, no expiry job, though the advisor's own prompt-building code already branches on `skipped`; (5) no scheduler exists anywhere in the repo (no `vercel.json` crons, no GH Actions schedule, no `pg_cron`) for any of the four `/api/jobs/*` routes — confirmed the deadline-reminders job itself works correctly end-to-end (first real run in this project's history, `external_sync_jobs` 0→1, correctly notified 0 since nothing crossed a threshold) once manually triggered via the admin panel, so this is a missing-infrastructure gap, not a code defect. Cross-cutting: near-zero test coverage on exactly the surface Phase 50 names (deadline math, status transitions) — only `__tests__/deadlines/ingest.test.ts` exists in-territory, and `__tests__/opportunities/readiness.test.ts` tests an unrelated same-named function, not `lib/applications/readiness.ts`'s `computeReadiness`. Positive findings, browser-verified end-to-end and working as claimed: application tracker (created a real application, 8-item checklist, readiness 0%→13%, 8-state status dropdown, all persisted), monthly review (honest "not enough history" empty state), notifications bell/mark-read, goals and time-budget/busy-mode (code + live DB evidence). Full detail and a 5-item ranked finish-order proposal for Package 2+ (item 1 of CEO's already-approved Package 2 excepted): `docs/feat2-loop-audit-2026-08-22.md`. | `docs/feat2-loop-audit-2026-08-22.md` (new). No app code touched this package — audit only, by design (Package 1 is diagnosis, not treatment; the one exception is already CEO-approved as a separate Package 2 PR). | **Package 1 complete, pushed, PR open — awaiting CEO verification.** Package 2 (the cycle-status fix) approved by CEO mid-audit and queued next as its own PR, smallest-possible-change scope, tests-pin-then-extend order, per CEO's explicit constraints. Items 2-5 of the ranked list await CEO's package-ordering call. |
| FEAT-1 | Claude (org lane per `docs/ORYN-ORG-BRIEFS.md` §FEAT-1, claimed 2026-08-22) | `oryn/feat1-eligibility-honesty` (isolated worktree, branched from `origin/main`@`38d7be7`) | **Package 1 — honest eligibility copy** (strategy priority #1, the live trust defect): empty `opportunities.eligible_countries` renders unresearched-but-possibly-restricted programs as eligible to everyone with zero warning, in both `lib/opportunities/matching.ts` `computeEligibility()` and `lib/counselor/eligibility.ts` `evaluateOpportunityEligibility()`. Design direction (per the brief's constraints, both verified in code before claiming): keep empty-means-open load-bearing for confirmed-open rows via a new structured marker (`opportunities.country_eligibility_confirmed_open`, migration 0060 — **written, NOT applied**; live migration list re-checked 2026-08-22, live DB ends at 0056 with 0057-0059 written-unapplied on `main`), and extend the existing advisory-notes pattern in both call sites so empty-and-unconfirmed rows carry an honest "not verified yet" note instead of silence. No rows marked ineligible. Unit tests at both call sites. | `lib/opportunities/matching.ts`, `lib/counselor/eligibility.ts`, `lib/opportunities/persist-matches.ts`, `supabase/migrations/0060_opportunity_country_eligibility_confirmed_open.sql` (**written, NOT applied**), `types/database.ts`, `lib/dev/fixtures.ts`, their tests (+11 new), `docs/product-decisions.md`, `docs/handoffs/feat1-eligibility-honesty-2026-08-22.md`. No live DB writes, no migration applied by this lane. | **Package 1 complete, PR open to `main` (not merged).** Design shipped: tri-state via `country_eligibility_confirmed_open` (default false, read defensively `?? false` so behavior is identical pre-application) + advisory unknown-notes in both call sites when empty-and-unconfirmed with no other eligibility signal; note suppressed for rows with structured citizenship lists or restriction prose (researched — "not verified" would be false); never `eligible: false`/`known_ineligible` — no rows marked ineligible. Gate green: lint clean, typecheck clean, 121 files/1,835 tests, build succeeds. Full detail: `docs/handoffs/feat1-eligibility-honesty-2026-08-22.md`. Follow-ups for others: apply 0060 (founder/coordinator), backfill the ~5 research-confirmed-open rows per-row with evidence (research org, after 0060), intake-time marker capture (data acquisition). Package 2 (admissionSystemType) not started — separate later assignment. |

| BUG-1 | Claude (org lane per `docs/ORYN-ORG-BRIEFS.md` §BUG-1, claimed 2026-08-22) | Package 1: `oryn/bug1-opportunity-data-defects` (branched from `origin/main`@`85c3d65`). Package 2: `oryn/bug1-requirement-program-ownership` (fresh worktree, branched from `origin/main`@`ce3ac74` — deliberately not stacked on package 1's branch). | **Package 1 — triage cycle 1**, the two live `opportunities` defects the founder pointed this lane at (Diamond Challenge duplicate pair, "3 garbled scrape rows"). **Diamond Challenge**: ran the codebase's own dedup across all 391 rows — only same-org pair above threshold in the whole corpus (blast radius 1), `cb1ae3e2` already `status='disabled'`, purpose-built detector `lib/opportunities/duplicates.ts` already scores it `1.0`/`deterministic`. Closed as already-correctly-handled, no code change. **Garbled rows**: the "3" was a ~28x undercount — **85 of 271 `status='active'` rows (31.4%) carry a description-quality defect**, root cause traced to the source Google Drive spreadsheet cells (2026-08-18 import), not any codebase transform. Built an ingest-time guard (CEO-approved ahead of the rest of triage, since BASORG has ~96 records queued behind verification through the same path); CEO split the 85 into Tier 1 (6 uncontested non-opportunities, routed to and actioned by RES-I2) and Tier 2 (~79, escalated to the founder). One self-correction mid-pass, recorded in `known-issues.md`: an initial claim about `scripts/import-opportunity-corpus.ts:198` was wrong and retracted before any fix was attempted. **Package 2**: closed a real, previously-deliberately-left-open gap — the admin "add a requirement" action never verified a submitted `program_id` belonged to the given `university_id`. New pure decision function `lib/requirements/program-ownership.ts` (mirrors `decideIngestion`'s pattern), wired into a live ownership check before insert. Both stale `known-issues.md`/`SECURITY.md` entries removed. **Process note**: mid-package-2, lost track of shell cwd after removing a worktree and one relative-path edit briefly landed in the primary shared checkout (org rule §3.4) — caught via the harness's own file-change notice, verified via `git status` that it was exactly one isolated uncommitted edit, reverted with a scoped `git restore`, redid the fix correctly inside the isolated worktree using absolute paths from then on. Never committed, pushed, or seen by another session. Full detail in the report to CEO. | Package 1: `lib/opportunities/description-quality.ts` (new), `lib/opportunities/ingest.ts`, 28 new tests, `docs/known-issues.md`, `data/audit/opportunities-description-defects-2026-08-22.md` (new). Package 2: `lib/requirements/program-ownership.ts` (new), `app/(app)/universities/[id]/requirement-actions.ts`, 4 new tests, `docs/known-issues.md`, `SECURITY.md`. No live DB writes in either package. | **Package 1: PR #16 open, awaiting CEO verification** (gate green: lint/typecheck clean, 1883/1883 tests, build succeeds). **Package 2: PR #23 open, awaiting CEO verification** (gate green: lint/typecheck clean, 1865/1865 tests, build succeeds). Idle pending CEO verdicts / next triage package — standing brief is to re-sweep `known-issues.md` fresh and self-select the next highest-impact reproducible defect not already owned by another lane. |

| RES-V-R3W2 | Claude (combined RES-V1+V2 verification lane per `docs/ORYN-ORG-BRIEFS.md`, claimed 2026-08-22) | `oryn/res-v-ecw2-verification` (isolated worktree, branched from `origin/main`@`d88db87`) | Verifying RES-R3's wave-2 eligible_countries research on `origin/oryn/res-r3-eligible-countries-w2`: `data/research/opportunities/ecw2_batch1.jsonl` (ECW2-001..012), `ecw2_batch2.jsonl` (ECW2-013..022), `docs/research/opportunities-eligible-countries/WAVE2_ECW2_2026-08-22.md`. Scope: contract/ID validation against the whole research corpus + live opportunity-row identity checks (read-only Supabase MCP); 100% re-verification of TechGirls' 37-country list (ECW2-017, the only DB-changing proposal); independent confirmation of the Türkiye Scholarships citizenship-exclusion finding (ECW2-020); source spot-check of all 14 confirmed-open-worldwide records; honesty audit (HOSA resolution, RISE dormancy, null-by-design vs RULE-ELIGIBILITY-009). Verifier posture: never edits researcher files, never writes to the live DB — defects go back as itemized reports. | `docs/research/verification/v_ecw2_verdict.md` + this row. No DB writes, no migrations, no app code. | **Complete, pushed. Batch verdict: PASS — cleared for ingestion, zero blocking defects (3 non-blocking observations itemized in the verdict).** TechGirls 37/37 exact against the live page (mechanical sorted diff, spelling conventions verified against live `SELECT DISTINCT`); Türkiye Scholarships exclusion independently confirmed verbatim on the official .gov.tr page ("Turkish citizens and individuals who have lost Turkish citizenship" ineligible) → **URGENT ingestion-priority flag for the prose fix**; all 14 confirmed-open records source-checked at 100% (verbatim, no paraphrase drift); RISE dormancy claims reproduced (301 + 404); HOSA supersession page verified name-by-name and the row's stale Germany/Italy description confirmed live; robots.txt checked before every browser-context fetch, no AI-crawler block routed around. |
| ORYN-BASORG | Claude (research org lead per `docs/ORYN-ORG-BRIEFS.md` §ORYN-BASORG, claimed 2026-08-22) | `oryn/basorg-org-status` (isolated worktree, branched from `origin/main`@`58ce0c5`) | **Management lane, not a research lane** — assigns every package the seven research sessions work on, and is the only session that moves a batch research→verify→ingest. Live map, escalations, and standing rules: `docs/handoffs/basorg-research-org-status-2026-08-22.md`. | Allocation, pipeline enforcement (researchers never write live; verifiers never edit researcher files; ingesters are the only live writers, I1 `university_*` / I2 `opportunities*`, territories never mix), ID namespacing before dispatch, ratcheting repeated findings into standing rules, protecting the evidence bar. Does no research itself. | **Active. 7 of 7 lanes staffed and running.** The earlier "3 of 7" was an addressing failure (session titles never propagated to `ListAgents`), not an absence — resolved via socket-address routing. **First full research→verify→ingest cycle completed**: RES-R3 researched wave 2, an independent lane verified PASS 22/22, RES-I2 applied, BASORG confirmed live. **Live trust defect CLOSED** — Türkiye Scholarships now discloses the Turkish-citizen exclusion (verified 11:03:09Z), TechGirls carries 37 verified countries (11:04:49Z); invariants held (391 total unchanged, empty-eligible_countries 352→351, exactly −1). Assignments: R1 Australia (37 unis/0 programmes, brief's 35 corrected; Adelaide identity question ruled — verify from official source, record both if a merger transition, defer rather than guess), R2 P2 summer_program, R3 wave 3 competitions, V1 DLOPP contract validation, V2 DLOPP source verification (14/14 dated records confirmed byte-exact by direct re-fetch), I1 corpus reconciliation, I2 opportunity ingestion. **Near-miss caught**: a zero-fabrication, independently-source-verified batch was one vocabulary coercion away from degrading live data — mapping `cycle_status` "unknown"→"unverified" would have stripped `open` and `closed` from 6 live rows. Fixed by RULE-INGEST-003 (information-monotonic writes, all columns). **Dartmouth's 53 NOT assigned** — checked before assigning and found gate-blocked (registrar-contracted vendor platform, same class as McMaster's 432). Three sessions hit permission blocks (CEO DB-write, R2 force-push, I2 PR-creation); BASORG declined to perform any of them on the blocked session's behalf and escalated as a founder capability question. Full detail: `docs/handoffs/basorg-research-org-status-2026-08-22.md`. |
| RES-R3 | Claude (research org lane, claimed 2026-08-22, coordinator-managed background agent reporting to ORYN-CEO) | `oryn/res-r3-eligible-countries-w2` (isolated worktree at `.claude/worktrees/agent-afacc0649dadcf830`, branched from `origin/main`@`38d7be7`) | Wave 2 of the `opportunities.eligible_countries` programme, Package 1 exactly as wave 1's own scoped plan defined it: the small high-stakes categories (remaining null `research`/`scholarship`/`fellowship`/`internship` rows — counts re-measured live before starting, not trusted from any snapshot) + wave 1's two named open threads (TechGirls' 37-participating-country list with genuinely different fetch approaches; Erasmus+ `citizenship_restrictions` prose-tightening proposal ONLY — its `eligible_countries` stays null by design per RULE-ELIGIBILITY-009) + one more resolution attempt on the inherited HOSA international-chapter-list conflict, outcome recorded either way. All wave-1 proven rules binding: zero title/keyword matching even for prioritization (measured 0% safe-apply rate); confirmed-open-worldwide keeps an EMPTY `eligible_countries` (confirmation recorded in research notes, never a fabricated all-countries list); populate only from explicit official statements, verbatim, with source URL + retrieval date; compound structures stay null with the structure documented; unresolved stays null with the reason. | `data/research/opportunities/ecw2_batch*.jsonl` (ID prefix `ECW2-`), `docs/research/opportunities-eligible-countries/**` (additive), this row. **Research output ONLY — this lane never writes to the live database** (read-only Supabase MCP for re-measurement and the country-spelling `SELECT DISTINCT`); ingestion is RES-I2's after RES-V1/V2 verification. PR opened after rebase onto `origin/main` (workstreams row conflict resolved additively — every lane row from both sides kept). | **Complete, pushed, PR open.** 22 records: `ecw2_batch1.jsonl` (ECW2-001..012), `ecw2_batch2.jsonl` (ECW2-013..022), notes in `docs/research/opportunities-eligible-countries/WAVE2_ECW2_2026-08-22.md`. Outcome: 1 populate proposal (TechGirls, 37 countries), 14 confirmed-open-worldwide (empty stays correct), 3 no-statement-found, 2 compound/null-by-design (RSI, HOSA), 1 unresolved (RISE), 1 prose-only (Erasmus+). Yield 1 populate / 20 rows (~5%) — matches wave 1's measured rate exactly; an honestly-resolved 5% is the expected outcome of this method, not a shortfall. RES-V verdict: **PASS 22/22, zero blocking defects**. Two findings routed to BASORG: Türkiye Scholarships live-row prose contradicts its own official source (urgent student-facing trust defect), TechGirls re-checked 37/37. Wave 3 (competition category) confirmed and started on `oryn/res-r3-eligible-countries-w3`. |
| ORYN-CFO | Claude (independent auditor/inspector, established 2026-08-22 by the founder; reports DIRECTLY to the FOUNDER, not to ORYN-CEO — the CEO is itself audited) | `oryn/cfo-workstream-claim` (isolated worktree at `.claude/worktrees/oryn-cfo`, branched from `origin/main`@`a7a554a`) | Standing audit rounds over the org: ownership integrity (workstream rows vs origin branches), scope discipline (diffs vs briefs), stalled/silent lanes, double-assignment/collision, claim decay (re-measuring doc numbers live), rule compliance (worktrees, gate, migrations, evidence bar, ID prefixes, robots.txt), permission-boundary integrity, and founder-queue accuracy. Round reports go to the founder; lane findings are copied to the owning lane's manager (ORYN-BASORG for RES-*, ORYN-CEO for the rest). Intro messages sent to all 14 org sessions 2026-08-22 ~11:40 local. | Docs-only writes: ORYN_WORKSTREAMS.md corrections (to measured reality), doc-contradiction records, docs/founder-blocked-backlog.md accuracy, own audit reports. NEVER: code, tests, migrations, config, live DB data, or another lane's branch — code/data defects are delivered as reports precise enough to act on, through the owning lane's manager. | **Active** — claim row + intros done; audit round 1 starting 2026-08-22. |

## Known cross-branch facts worth not re-discovering

- `origin/main` tip is `5c59115` ("docs: coordination integration — workstreams map +
  re-measured current-state"), merged from `oryn/coordination-integration-2026-08-20`. DATA-A
  has merged this into `oryn/programs-pipeline-reconciled`. Neither DATA-A nor PROD-B's
  product work has been merged into `main` yet as of this note.
- **Migration numbering, corrected by PROD-B, still current**: PROD-B's branch has migrations
  through **0048** (`0047_structured_eligibility_facts.sql`,
  `0048_profile_view_visibility_guard.sql` — both written, reviewed, **not yet applied
  live**). **DATA-A should use 0049+** for any new migration — DATA-A has not added any
  migrations this session, so this remains purely a heads-up, not yet consumed.
- **Migration 0043 — fully resolved 2026-08-20**: DDL live (found by Claude B), data backfill
  run and verified by DATA-A (`4dd66cd`) — 9/9 pairs correctly `duplicate_status='superseded'`
  with matching `superseded_by_id`, confirmed against `lib/universities/
  duplicate-supersessions.json` and re-verified live again by Claude B during merge-prep.
  `0046` also confirmed live. **What's left is application code, not data or DDL**:
  `lib/universities/canonical.ts`'s 16 read paths still use the JSON snapshot instead of
  querying the now-correct DB columns — scoped as a post-integration follow-up in
  `docs/handoffs/claude-b-merge-plan-2026-08-20.md`, deliberately not started (async-boundary
  refactor, real scope, not opportunistic cleanup).
- **Migration 0047 had a real live-crash bug, now fixed**: `lib/opportunities/
  persist-matches.ts` named `citizenship_countries`/`eligible_citizenships` in an explicit
  Supabase column list — PostgREST rejects the whole query (42703) when a named column is
  missing, so `refreshOpportunityMatches()` (unconditional on every `/opportunities` page
  load, no try/catch) crashed the entire page on any environment without 0047 applied,
  including this one. Fixed by Claude B (`5c1d4e4`, `select("*")`, same pattern as `08ddf0f`'s
  earlier fix for the identical failure mode). Confirmed live before/after against
  `oryn-qa-scratch`.
- **Still open, unchanged**: a new Supabase-secret-key regression ("JWT issued in future"),
  full detail in `docs/current-state.md`. Neither 0047 nor 0048 applied live yet.
- DATA-A's machine has no local `SUPABASE_SECRET_KEY`/`ANTHROPIC_API_KEY`/`TAVILY_API_KEY` —
  all its DB writes went through the Supabase MCP tool (`execute_sql`) directly against
  `qtcvcflzxbuagvvwahhu`, and its research went through `WebFetch`/`WebSearch` rather than
  this repo's own Tavily integration. A session with real credentials can run
  `scripts/acquire-programs.ts`/`scripts/ingest-university-programs.ts` directly instead.
- **Turkish school registry (`canonical_entities` where `entity_type='school'` and
  `country_code='TR'`) is further along than any doc currently states**: 58 schools live,
  133 aliases, `school_profiles` populated for all 58, and `school_credentials` already
  carries 42 IB Diploma Programme rows plus assorted AP/Cambridge IGCSE/German
  Abitur-DSD/French Baccalaureate-equivalency rows — re-measured directly 2026-08-20 via
  Supabase MCP `execute_sql`, not copied from `docs/entity-canonicalization-audit.md`
  (which predates it and undercounts at "54 of 58"). `entity_verification_queue` also has
  ~50 more school rows in `queued`/`in_progress`/`verified` states from that same
  2026-08-15/17 pass — 8 of them (`Aka Koleji`, `AlJazari International School of Science
  and Technology`, `Anka Bilim Koleji`, `Ankara ABC Okulları`, `BALIKESIR ACI COLLEGE`,
  `Cakir Schools`, `Yeni Yol Schools`, `ZAFER COLLEGE`) have no matching `canonical_entities`
  row yet — genuinely unresolved, not just unverified. Nobody currently owns finishing this
  queue or extending the school registry further; the RESEARCH row above is picking up
  exactly that (resolving the 8 open queue names plus new geographic coverage), as a
  research handoff, not a live write.
