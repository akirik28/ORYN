# ORYN Workstreams — live ownership map

**Purpose:** prevent duplicate work across concurrent Claude agents. This file answers
*who owns what, on which branch, right now* — nothing else. It is rewritten in place per
workstream row, not appended to as a log.

**This is not a duplicate planning system.** The other three canonical documents already
exist under different names and are not reproduced here:

- **Master plan / product direction** → [`docs/MASTER-EXECUTION-STRATEGY.md`](./MASTER-EXECUTION-STRATEGY.md)
- **Operational status / live coverage numbers** → [`docs/current-state.md`](./current-state.md)
  (rewritten at each integration checkpoint, not appended to — check its own "last
  measured" date before trusting it; a stale copy is worse than no copy)
- **Permanent decision log** → [`docs/product-decisions.md`](./product-decisions.md)

If you're about to create `ORYN_MASTER_PLAN.md`, `ORYN_STATUS.md`, or `ORYN_DECISIONS.md`,
stop — those roles are already filled by the files above.

## How to use this file

Before starting a substantial new package: `git fetch --all --prune`, re-read this file,
confirm nobody already owns what you're about to do. Claim your row (update it) before
starting large work; update it again when you finish or hand off. Do not rewrite another
agent's row from assumption — verify from git log/live DB first, then correct with
evidence if it's actually wrong.

## Current rows (rewritten 2026-08-21 by the Integration Manager against verified `main`@`bd1a15c` — every row below checked from git ancestry, live DB queries, or the branch's own handoff doc; nothing carried forward from the prior version without re-checking)

| Workstream | Owner | Branch | Current package | Scope | Status |
|---|---|---|---|---|---|
| DATA-A | Claude A | `oryn/programs-pipeline-reconciled` | Admissions-URL coverage campaign + programme-catalogue acquisition pipeline (extraction-engine fixes for MBChB/integrated-master's/CertHE degree tokens, a bare-"Diploma" false positive, a URL entity-decoding bug) + a dry-run ingestion staging tool + a large volume of `data/research/**` batches. | University/opportunity data acquisition, canonical graph, ingestion pipelines, data QA. | **MERGED into `main`** at tip `7f27734` (commit `165486f` on `main`). Integration Manager ran the full gate on that exact tip before merge: lint/typecheck clean, 1155/1155 tests, build clean (39 routes), zero new migrations (`0049+` confirmed genuinely free), diff classified as 11 files / 1319 lines of real code entirely scoped to the acquisition engine — no overlap into counselor/scoring/eligibility/canonicalization. Live-verified after ingestion ran: `university_programs` 664 → **4,539 rows across 99 universities**, **4,636** audit rows in `program_research_queue`. Historical/complete unless the branch resumes. |
| PROD-B | Claude B | `oryn/counselor-data-quality-v1` | B1-B12 product-integration/data-contracts/counselor-surfacing/UI-foundation/QA package, including a real live-crash fix (`refreshOpportunityMatches` 42703-erroring on any env without migration 0047). | Counselor intelligence, product/UX, opportunity data quality, canonical entity integration. | **MERGED into `main`** (commit `8a265fe`). Historical/complete. |
| COORD | Claude B | `oryn/coordination-integration-2026-08-20` | Coordination-only package: workstreams/current-state doc reconciliation. | Docs only. | **MERGED into `main`** (`5c59115`). Historical/complete. |
| RESEARCH | Research Claude | `oryn/research-turkey-schools` | Five packages: Turkey high-schools canonical registry (Wave 1, 100/100), secondary-education-system intelligence (8 systems), country-level admissions-system intelligence (6 destinations), opportunity-eligibility intelligence (grade/age normalization, citizenship taxonomy, extraction audit), and a counseling-materials discovery-list gap closure. | `docs/research/**`, `data/research/schools|academic-systems|opportunity-eligibility/**` — research/evidence only. | **MERGED into `main`** (part of `5ec6700`). Historical/complete. |
| ADMISSIONS-INTEL | Claude (overnight research session) | `oryn/admissions-intelligence-research` | R3.1 continuation: added 8 countries (Switzerland, France, Spain, Ireland, Australia, New Zealand, Hong Kong, Singapore) to the existing 6-country package — 14 countries total in `admissions-systems-v1.json` (v2 internal marker), README cross-country matrix fully integrated, `RULE-ADMISSIONS-013`–`016` added. Batch 3 (Sweden/Belgium/Austria/Poland/Czech Republic) scoped but explicitly not started per founder pause instruction. | `docs/research/admissions-systems/**`, `data/research/admissions-systems/**` — research/evidence only. | **MERGED into `main`** (`9d32a54`). Complete for its authorized scope; genuinely idle, no pending batch. This dataset is now a dependency for GEOGRAPHY-CONDITIONAL-WEIGHTING below. |
| OPPS-NIGHT | Claude (overnight research session) | `oryn/night-opportunities-research-2026-08-21` | Global opportunities-intelligence campaign filling category gaps (hackathon/conference/student_program were at zero) and geography gaps (Middle East/East Asia/South Asia/ANZ were at zero). 51 page-verified records across 4 batches. One slice (international olympiads) never completed — check `night1_2026-08-21_groupOLYMPIADS.jsonl` for partial output before re-dispatching. Wave 2 never dispatched (ran out of time). Cross-checked against DATA-A's concurrent opportunity research — zero real duplicates found. | `data/research/opportunities/night1_*.jsonl` and tracker doc — research/evidence only. | **MERGED into `main`** directly (`3d5367d`) — superseding the prior note that it was "left for DATA-A to integrate"; it landed straight into `main` in this wave instead. Live-verified: `opportunities` table now at **369** rows. Not yet ingested into any downstream matching/eligibility pipeline. |
| COUNSELING-REGISTRY-UNIFICATION | Claude (overnight session) | `oryn/counseling-registry-unification` | **Proposal only — nothing executed.** Built a collision map between the two competing counseling-intelligence rule registries (see COUNSEL-RESEARCH row): 33 colliding rule IDs, all independently verified to be genuinely different content (not duplicates, a pure numbering clash) plus a deeper provenance problem — this branch's own `08-unsafe-inference-rules.md` cites rule numbers as "(peer)" with paraphrases that were checked against peer's actual content and found roughly half-accurate; the underlying `001`–`033` block plus 7 same-numbered sources (empty `used_in`) most likely trace to an unidentified earlier session's file, never independently verified. Proposes a 172-rule / 130-source canonical set (peer's 001–123 + this branch's 200–246/901–902 kept as-is, the 33 colliding IDs quarantined, nothing discarded) and flags one schema-convention difference (peer's rules carry a structured `sources` array, this branch's don't). | `data/research/counseling-intelligence/rules.canonical-proposal.json`, `sources.canonical-proposal.json` — touches neither underlying registry. | **MERGED into `main`** (`accc69f`). This is a roadmap for reconciliation, not the reconciliation itself — the 33 excluded rules and 7 flagged sources explicitly need a dedicated human review pass before being trusted at the same confidence as the rest. |
| GEOGRAPHY-CONDITIONAL-WEIGHTING | Claude (overnight session) | `oryn/geography-conditional-weighting` | Forked from the 013956 counseling lineage. Used ADMISSIONS-INTEL's 14-country dataset (existence and content independently verified before citing it, including reading the UK/Germany files for sourcing discipline) to write an implementation-facing design spec for making admission-outlook scoring geography-conditional: a two-gate mechanism, per-system specs for US/UK/Turkey/Germany, `RULE-COUNSEL-124`–`129`. Deliberately proposes **no numeric weights**, per `AGENTS.md`'s own standing prohibition on LLM-invented scoring parameters — this is a design spec, not an implementation. Honestly flags its own gaps: UK only partially cross-validated, Netherlands available but not used (Germany chosen instead for stronger cross-check material), Turkey not covered by the 14-country dataset at all (relies on this session's own prior sourcing). | `docs/research/counseling-intelligence/18-geography-conditional-scoring-design-spec.md` | **MERGED into `main`** (`a239121`). Design-only — see COUNSELOR-LOOP-QA below for confirmation that the underlying code (`lib/admissions/outlook.ts`) has *no* geography input today, which is exactly the gap this spec is meant to close. An engineering lane still needs to make the actual qualitative→numeric conversion decision. |
| COUNSELOR-LOOP-QA | Claude (overnight session) | `oryn/counselor-loop-qa` | Ran 4 synthetic personas (incl. a Turkey/YKS-track case built specifically to stress-test geography-blindness) through the real, deterministic scoring/matching/outlook engines — no DB, no network, no AI calls. **Found 5 real defects in shipped code**, most importantly: (1) `computeAdmissionOutlook` has zero geography/admissions-system input (`lib/admissions/outlook.ts:60-87`) — every persona gets a US-holistic-style read regardless of target country; a 92/100-GPA Turkey-YKS-track student is told `extreme_reach` with no representation of their actual exam-placement standing; (2) `scoreAcademics` structurally cannot score rigor for non-Anglo-American/IB curricula and never reads per-course grades (`lib/scoring/dimensions/academics.ts`); (3) `computeRelevanceScore` does raw substring matching, so `"Computer Science"` false-positively matches any `"science"`-tagged opportunity at relevance=100 (`lib/opportunities/matching.ts:126-130`); (4) "weakest 3 dimensions" tie-breaking is array-declaration-order, not semantic; (5) the unweighted 9-dimension average likely under-scores realistically-focused strong students — the founder's own Phase 8.3 worked example scores 21/100 overall. Also confirmed working correctly: no crashes on sparse data, cross-curriculum GPA refuses to fabricate a conversion, country-gating works both directions, research scoring genuinely doesn't require publication. | `scripts/qa-counselor-loop.ts` (re-runnable QA harness, no side effects), `docs/handoffs/counselor-loop-qa-report.md` + raw output. | **MERGED into `main`** (`5479e81`). Integration Manager found 3 `no-explicit-any` lint errors in the merged script (the fixture data's real shape didn't match `computeCompleteness`'s declared parameter types) and fixed them directly with properly-typed helpers rather than casts — `bd1a15c`, full gate re-confirmed clean, 1155/1155 unchanged. **The 5 defects above are real, unfixed, reproducible with `npx tsx scripts/qa-counselor-loop.ts`, and worth the founder's attention** — none require credentials. |
| COUNSEL-RESEARCH (two lineages) | Two independent overnight sessions | `oryn/counseling-intelligence-research` (interleaves a second, docs-only "canonical-entity-intelligence" mandate) and `oryn/counseling-intelligence-research-013956` (peer session) | Two large, independent counseling-intelligence research packages that collided before establishing an ownership split: major-family evidence taxonomy across 20 subject families, explainability framework, persona testing, geography/admissions intelligence, executive summary (013956/peer side); overview, development taxonomy, opportunity-development mapping, plus a separate canonical-entity-intelligence package (17 documents: identity framework, alias taxonomy, duplicate-detection/unsafe-auto-merge rules, etc.) on the other side. | `docs/research/counseling-intelligence/**`, `docs/research/canonical-entity-intelligence/**`, `data/research/counseling-intelligence/**` — research/evidence only. | **Both branches MERGED into `main`** (`3a5c63f`). **One conflict deliberately left unresolved**: `00-overview.md` and `02-opportunity-development-mapping.md` collided; peer's version (013956) was kept at the canonical path per the two sessions' own agreed ownership split and peer's own account that its version was the stronger, schema-grounded one — the other version is preserved as `*.ALT-main-lineage.*`, nothing discarded. Full reasoning: `docs/research/counseling-intelligence/CONFLICT-NOTICE-00-and-02.md`. **Do not treat either file as sole source of truth for these two topics** until a reconciliation pass (see COUNSELING-REGISTRY-UNIFICATION above for the parallel rules/sources version of the same problem — independently verified: peer's registry is 129 rules/66 sources, this branch's is 82 rules/66 sources) produces one merged document/registry per topic. |
| UI-SIMPLIFY | Claude (third session) | `oryn/ui-simplification-v1` | UX simplification: dashboard hierarchy pass, sticky profile jump-nav, citizenship settings form, Discovery/Map package, Opportunity Detail Experience V1, a dev/QA test-account script. Has merged PROD-B's entire branch in twice — confirmed via `git merge-base --is-ancestor`. | UX research, cognitive-load reduction, progressive disclosure, final information architecture. | **NOT merged — reported founder-rejected on product grounds**, 2026-08-21, relayed through the coordination session ("that UI is very bad, continue with the old one") — **not independently confirmed by the Integration Manager directly with the founder**, recorded as-reported. Latest observed HEAD `01231d2` (branch was still moving as of last check; not merged, not deleted). Do not resume work on this branch without a direct founder decision, not a relayed one. |
| (unclaimed/dormant) | — | `oryn/university-intelligence-spine`, `oryn/counselor-core-v1`, `oryn/integration-2026-08-19`, `oryn/recovery-pre-integration-2026-08-19`, `oryn/product-ux`, `oryn/programs-opportunities-intel`, `oryn/programs-pipeline` | — | Confirmed via ancestry checks earlier this session: no commits ahead of what's already in the merged lineage above — historical/superseded, don't build on any of these without first confirming nothing has landed on `main` since. |

## Known cross-branch facts worth not re-discovering

- **`main` tip is `bd1a15c`** ("fix(qa): replace any casts with properly typed fixture helpers") — verified directly, not copied. Full gate re-run against this exact commit by the Integration Manager: lint clean, typecheck clean, **1155/1155 tests**, build clean (39 routes).
- **Migrations, reconfirmed live via `list_migrations` this checkpoint**: `0043`/`0046` applied. `0047_structured_eligibility_facts.sql` and `0048_profile_view_visibility_guard.sql` are present in `main`'s file tree (came in with PROD-B) but **still not applied to the live database**. DATA-A's merge added zero new migrations — **`0049+` remains genuinely free**.
- **Live DB counts, re-measured directly this checkpoint** (`oryn-qa-scratch`, `qtcvcflzxbuagvvwahhu`): `universities` 1,019 · `university_programs` **4,539** across **99** universities (up from 664 pre-ingestion) · `program_research_queue` **4,636** audit rows · `opportunities` **369**.
- **Credentials**: per the prior checkpoint's note (not independently re-verified in this pass — flagging the provenance rather than re-asserting it as freshly confirmed), the founder supplied a working `SUPABASE_SECRET_KEY`, unblocking the secret-key-dependent write path. **`ANTHROPIC_API_KEY` and `TAVILY_API_KEY` remain empty** — every AI-backed feature (advisor chat, weekly-plan generation, CV extraction, AI-structured opportunity/requirement data) and all Tavily-based discovery are still degraded or dead. See the Integration Manager's release-readiness pass for exactly which routes fail hard vs. degrade gracefully.
- **Two unresolved research conflicts, both deliberate, both documented, neither silently picked**: the counseling-intelligence `00`/`02` file collision and the counseling rules/sources registry collision (see COUNSEL-RESEARCH and COUNSELING-REGISTRY-UNIFICATION rows). Anyone consuming counseling-intelligence research for a product decision should read both sides of each, not assume the canonical-path file is the only one that matters.
- **A real, unfixed defect set exists in shipped scoring/matching/outlook code**, found by live persona runs, not just documented gaps — see COUNSELOR-LOOP-QA row. Most consequential: admission-outlook scoring is geography-blind (no country/admissions-system input at all), and opportunity relevance-matching has a substring false-positive bug.
- **Turkish school registry** (`canonical_entities` where `entity_type='school'` and `country_code='TR'`): 58 schools live, 133 aliases, `school_profiles` populated for all 58 — carried forward from the prior checkpoint's live measurement, not re-verified this pass; re-check before citing as current if this becomes load-bearing for a decision.
