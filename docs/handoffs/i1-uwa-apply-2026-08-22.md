# RES-I1 apply — UWA, 2026-08-22

**Package I1-10, approved by ORYN-BASORG.** UWA goes from 0 to **107 programmes**. This
closes the Australian corpus: five universities, **651 total** (UNSW 217, Sydney 149,
Monash 178, UWA 107) — Adelaide (119) remains unextracted for ingestion, unaffected by
this package.

## Batch ID — the revert path, recorded prominently as the condition of approval

```
au_programs_uwa_2026-08-22.jsonl_2026-08-22
```

Lives on `program_research_queue.batch_id` → `promoted_program_id` (not a column on
`university_programs` itself). Verified directly: exactly 107 rows under this batch_id, all
`outcome='accepted'`, all `promoted_program_id` non-null, **107 distinct promoted ids** —
clean 1:1, zero orphans.

## Procedure (mandatory, all 7 steps)

1. **Re-measured live immediately before writing**: `university_programs` **16,663**, UWA 0,
   Adelaide 0, UNSW 217, Sydney 149, Monash 178, Glasgow 101, Edinburgh 98, Waterloo 107,
   `opportunities` 391 — every number matched the required starting state exactly.
2. **Scope reconfirmed on the file already checked out and asserted in I1-9**
   (`au_programs_uwa_2026-08-22.jsonl`): 107 lines exact, single university name ("The
   University of Western Australia"), Adelaide's file confirmed still never checked out.
3. **Dry run via the real script, once more**: `{ accepted: 107 }` — exact match to I1-9.
4. **Applied**: `Inserted 107/107 row(s) into university_programs.` Zero insert errors, zero
   orphaned program rows in the script's own output.
5. **Re-verified live**: `university_programs` 16,663 → **16,770** (+107 exactly). UWA
   0 → **107**.
6. **Idempotency re-confirmed**: re-ran the identical file — `{ duplicate: 107 }`. A second
   `--apply` would insert nothing.
7. **Audit trail verified directly**: see batch ID section above — 107/107, clean, no
   orphans, no double-promotions.

## Invariants — what did NOT change

| | Before | After |
|---|---|---|
| UNSW | 217 | **217** |
| Sydney | 149 | **149** |
| Monash | 178 | **178** |
| Adelaide | 0 | **0** |
| Glasgow | 101 | **101** |
| Edinburgh | 98 | **98** |
| Waterloo | 107 | **107** |
| `opportunities` (total, cross-table) | 391 | **391** |

Total delta: **+107, exactly**. Every other count held. Adelaide's file was never read by
this package at any point, consistent with its 0 remaining exactly 0 by construction, not
merely by outcome.

## Net effect

`university_programs`: 16,663 → **16,770**. Australia: 37 universities, **651 programmes**
across the five verified universities (UNSW 217 + Sydney 149 + Monash 178 + UWA 107),
Adelaide's 119 still unextracted for ingestion. `university_requirements` and
`university_deadlines` untouched — this package's scope was `university_programs` only.
