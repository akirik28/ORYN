# RES-I1 apply — Australia (UNSW/Sydney/Monash), 2026-08-22

**Package I1-8, approved by ORYN-BASORG.** Australia goes from 37 universities / 0
programmes to 37 universities / **544 programmes**. UWA's 107 and Adelaide remain
excluded, unaffected.

## Batch ID — the revert path, recorded prominently as the condition of approval

```
i1-au-approved544_2026-08-22.jsonl_2026-08-22
```

Every one of the 544 rows this package wrote carries this `batch_id` in
`program_research_queue`. If any of them are found to be wrong, this batch_id alone
identifies exactly the 544 rows to remove — verified below to be exactly 544, no more, no
fewer, with a clean 1:1 mapping to `promoted_program_id`.

## Procedure (mandatory, per BASORG's I1-8 assignment — all 7 steps)

1. **Re-measured live immediately before writing**: `university_programs` **16,119**,
   Glasgow 101, Edinburgh 98, Waterloo 107, McGill 0, UNSW/Sydney/Monash all 0 — every
   number matched the required starting state exactly.
2. **Scoped file built and asserted before anything ran**: concatenated the same 3 files
   I1-7 already verified (`au_programs_unsw_2026-08-22.jsonl` 217,
   `au_programs_sydney_2026-08-22.jsonl` 149, `au_programs_monash_2026-08-22.jsonl` 178)
   into one file, `i1-au-approved544_2026-08-22.jsonl`. Confirmed exactly 544 lines and a
   university-name allowlist of exactly the 3 approved names — zero mentions of UWA or
   Adelaide anywhere in the file, checked directly before proceeding.
3. **Dry run via the real script**: first attempt hit a transient `HTTP 401` on
   `entity_external_ids` (Supabase-side, not the harness's safety classifier — the retry
   used the identical credential and code with no changes and succeeded cleanly).
   Retried once, per the standing "check whether transient before escalating" discipline;
   second attempt returned `{ accepted: 544 }` — exact match to I1-7's dry-run, no
   duplicate, no other outcome. Recorded here in case this is the same class as the
   already-tracked Supabase secret-key regression from earlier today rather than a fluke.
4. **Applied**: `Inserted 544/544 row(s) into university_programs.` Zero insert errors,
   zero orphaned program rows in the script's own output.
5. **Re-verified live**: `university_programs` 16,119 → **16,663** (+544 exactly). UNSW
   0→**217**, Sydney 0→**149**, Monash 0→**178** — every number matches BASORG's stated
   expectation exactly.
6. **Idempotency re-confirmed**: re-ran the identical scoped file through the dry-run
   path — `{ duplicate: 544 }`. A second `--apply` of this same file would insert nothing.
7. **Audit trail verified directly**, not assumed from the script's summary line:
   `program_research_queue` under `batch_id='i1-au-approved544_2026-08-22.jsonl_2026-08-22'`
   has exactly 544 rows, all `outcome='accepted'`, all `promoted_program_id` non-null,
   **544 distinct promoted ids** — clean 1:1, zero orphans, zero double-promotions.

## Invariants — what did NOT change

| | Before | After |
|---|---|---|
| Glasgow | 101 | **101** |
| Edinburgh | 98 | **98** |
| Waterloo | 107 | **107** |
| McGill | 0 | **0** |
| UWA | 0 | **0** |

Every non-Australian, non-target count held exactly. Total delta: **+544, exactly** —
16,119 → 16,663, matching 217+149+178 to the row. UWA's file was never read by this
package at any point (confirmed at scoping time, §2 above) — its 0 live rows are untouched
by construction, not merely by outcome.

## Why the apply was approved despite BASORG's earlier statement it might hold for the founder

Per BASORG's own message: these 544 cleared both verifier lanes independently, including
source-truth verification (RES-V2: 82/82 sampled fields clean) — the specific check UWA's
own corpus never received before its 63% defect rate was found. Treated as an insert
through the existing, tested path rather than new write machinery, consistent with
ORYN-CEO's ruling that Path A (the not-yet-built UPDATE-by-id mechanism) is what needs to
wait for the founder, not ordinary verified inserts.

## Net effect

`university_programs`: 16,119 → **16,663**. Australia: 37 universities / 0 programmes →
37 universities / **544 programmes** (UNSW 217, Sydney 149, Monash 178). UWA's 107 and
Adelaide remain unextracted/unverified/unapplied, exactly as before this package.
`university_requirements` and `university_deadlines` untouched (this package's scope was
`university_programs` only).
