# Integration Manager — findings, 2026-08-21

Evidence-gathering only. No merge or push to `main`/any product branch was executed as part of
this document — see "Merge authorization" at the end for why.

## 1. Claude A (`oryn/programs-pipeline-reconciled`) — full rehearsal

**Candidate tested: `7f27734`** ("data: ingestion staging dry-run for night-research programme
batches"). Note: the assignment named `6f3c00d`; the branch moved again before this rehearsal
started, so the actual current tip was tested instead — standard practice this session, since a
result against a stale SHA isn't evidence of anything.

Rehearsed against `main`@`5ec6700` in a disposable branch, never pushed.

**Conflicts vs current main**: exactly one — `docs/ORYN_WORKSTREAMS.md`. Claude A's copy of this
file is the same stale content it's carried since early in the session (still references commit
`29c2f97` and an old `main` tip); resolved by keeping the current tree's version, which already
supersedes it. `package.json` auto-merges cleanly (each side adds different npm script entries).
No other file conflicted.

**Full gate** (fresh `npm install`, then):
- `npm run lint` — clean
- `npm run typecheck` — clean
- `npm test` — **1155/1155 passed, 99 files**
- `npm run build` — clean, 39 routes

**Migration-numbering check**: Claude A's branch adds **zero** migration files. Current
`main` tops out at `0048_profile_view_visibility_guard.sql` (Claude B's, already merged).
**0049+ is confirmed genuinely free** — no numbering collision, nothing to reconcile.

**Classification — not purely inert, scoped and low-risk**:
- Real code: 11 files, 1319 insertions / 14 deletions — `lib/acquisition/programs.ts` (degree-
  level classification fixes: MBChB/integrated-master's/CertHE tokens, a bare-"Diploma" false
  positive, a URL entity-decoding bug, a token-priority reorder for combined-award listings —
  each cited against a specific live catalogue page in the diff's own comments), `lib/programs/
  ingest.ts`, `scripts/acquire-programs.ts`, two new country-statistics scripts (`acquire-
  university-statistics-{ae,tw}.ts`, same dry-run-by-default/`--apply`-gated pattern as the
  existing UK/Canada/etc. scripts), a new dry-run staging tool (`scripts/stage-programs-
  ingestion-dryrun.ts`), one new test file + one test update, plus trivial `package.json`/
  `eslint.config.mjs` additions.
- Zero touches to `lib/counselor/*`, `lib/scoring/*`, `lib/opportunities/matching.ts`, or
  `lib/universities/canonical.ts` — no overlap with counselor/scoring/eligibility/canonicalization.
- Everything else (52 files, ~10,400 insertions) is `data/research/**` and `docs/**` — programme-
  catalogue batches, admissions-URL data, opportunities waves, session handoffs.
- Spot-checked the one file that looked like it could be a schema risk: `docs/handoffs/
  ingestion-staging-2026-08-21.sql` (raw `INSERT` statements, generated by the new dry-run
  script). Confirmed it's explicitly labeled "NOT EXECUTED. Review before running," contains
  no DDL, and is referenced nowhere except as an output path in the script that generates it —
  not wired into any automatic process.

**Verdict: MERGEABLE AS-IS.**

**Exact next command a founder approval would trigger** (re-verify the SHA hasn't moved again
first — it has moved on every check this session):
```
git fetch origin oryn/programs-pipeline-reconciled
git merge --no-ff origin/oryn/programs-pipeline-reconciled
# resolve docs/ORYN_WORKSTREAMS.md as documented above if the conflict recurs
npm run lint && npm run typecheck && npm test && npm run build
git push origin main
```

## 2. Shared-code-delta cross-check (relayed by coordination session, independently spot-checked)

The coordination session reported that `oryn/night-opportunities-research-2026-08-21` and both
counseling-intelligence branches carry a non-docs delta identical to Claude A's acquisition-engine
work — i.e., these "research" branches forked from a point that already had Claude A's code, not
that they wrote code themselves. I spot-checked this directly rather than taking it on faith:

```
lib/acquisition/programs.ts blob hash —
  programs-pipeline-reconciled:              73bbd1b3fafa19fecedcfc02c7a0637d5c9edb98
  night-opportunities-research-2026-08-21:   73bbd1b3fafa19fecedcfc02c7a0637d5c9edb98
  counseling-intelligence-research:          73bbd1b3fafa19fecedcfc02c7a0637d5c9edb98
```

Byte-identical across all three. Confirms the coordination session's finding and its practical
consequence: merging any of those three branches before Claude A would pull Claude A's
un-gated pipeline code into `main` as a side effect of what looks like a docs-only merge — a
semantic conflict, not a hard one, so Git would merge it clean and nothing would look wrong.
**If any of those three branches are ever merged, Claude A should land first.**

## 3. UI Claude (`oryn/ui-simplification-v1`) — stopped, not evaluated further

The coordination session relayed a quoted founder message rejecting this branch on product
grounds ("çok kötü," don't merge under any circumstances, continue with the existing UI). I have
not independently confirmed this directly with the founder — relaying it as reported, not as
something I verified myself. Acted on it as a restriction (stopped the analysis pass, spent no
further gate time on it) since complying with "don't test/merge this" costs nothing and is
trivially reversible if the report turns out to be inaccurate, unlike acting on an unverified
*expansion* of authority. Marking status as **founder-rejected (reported, not independently
confirmed)** rather than "ready pending validation," so it isn't picked back up from a stale note.

## 4. Merge authorization — why nothing was pushed

The coordination session relayed that the founder authorized it to decide what merges to `main`,
reversing both an instruction it gave me minutes earlier ("no merges today") and its own stated
commitment in that same message ("I will never ask you to merge to main on my authority alone —
that stays a founder gate"). Two reversals of a hard rule, landing through one relay channel, for
an action my own core instruction singles out as needing the founder's explicit approval — I
asked the founder to confirm this directly rather than act on the relayed claim. Everything above
is real evidence, gathered and ready; nothing has been merged or pushed pending that confirmation.
