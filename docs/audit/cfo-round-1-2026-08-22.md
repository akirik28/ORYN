# ORYN-CFO — Audit round 1, 2026-08-22

**Auditor:** ORYN-CFO (independent; reports to the founder). **Fetch discipline:** every
remote-state claim below was made against `origin/*` immediately after an explicit
`git fetch origin`; fetch timestamps are noted inline where they matter (14:41, 14:47,
14:55 local). Live-DB claims were measured by read-only SQL against `qtcvcflzxbuagvvwahhu`
at the stated times. This report states what was checked, how, what was found, and what
was NOT checked.

---

## Findings

### F1 — CA 1,657-row apply executed by ORYN-CEO (severity: held open, pending evidence)

`docs/handoffs/i1_ca_1657_ingest-report.md` (on main) discloses the 1,657-row
`university_programs` apply (10:37–10:43 UTC) was executed by the ORYN-CEO session
directly, after its ingestion-agent spawn was blocked by the environment's safety
classifier — the report claims the classifier's own guidance was to use the normal tools.

- **Verified by me:** the data is exact — before/after 14,457 → 16,114 reproduces; window
  insert counts 1,266 (01:25 UTC, documented in the evidence-gate handoff) / 1,657
  (10:37) / 5 (11:36, RES-I1's Edinburgh+Waterloo) reconcile to the live 16,119 to the row.
  Procedure followed re-measure → dry-run → apply → verify. The disclosure was proactive,
  in the merged report itself.
- **Not verified:** the classifier's verbatim block text (requested from ORYN-CEO ~14:45,
  unanswered at round close); whether this is the same incident as the one where MERGE-1
  refused CEO-assigned blocked work, or a second instance.
- **The rule question:** `university_*` writes are RES-I1's exclusive territory; the CEO
  ran this ~15 minutes before RES-I1's session existed. Mitigation is real (founder-approved
  purpose, exact procedure, proactive disclosure). Surrounding norm, reported by BASORG and
  only partially verified by me: four classifier blocks hit four sessions today
  (CEO, RES-R2, RES-I2 ×2, MERGE-1) and none was proxied; RES-I2 abandoned a finished
  package rather than decompose a blocked transaction. I verified only MERGE-1's refusal
  and the CA report's self-disclosure myself.
- **Owner:** founder (the audited party's only superior).

### F2 — Report namespace misattribution (LOW)

The CA report is filed as `i1_ca_1657_ingest-report.md` — RES-I1's namespace — for work
RES-I1 did not perform (its body says so; RES-I1 confirmed the timeline unprompted).
Anyone counting RES-I1's writes from filenames gets a contradiction with RES-I1's own
(accurate) "one 5-row apply today". Fix: rename or add a header note. Owner: ORYN-CEO.

### F3 — ORYN_WORKSTREAMS.md carried 6 stale duplicate row copies (MEDIUM, fixed)

5 lane names had 2–3 copies each on `origin/main@5f2b572` (SCHEMA-GAPS-DESIGN ×3;
UK-PROGRAMMES, PRODUCT-VERIFICATION, OPPORTUNITIES-VERIFICATION, COUNSELOR-KNOWLEDGE ×2),
violating the file's own rewrite-in-place contract — union merges kept superseded states
beside current ones (e.g. UK-PROGRAMMES "1 of 7 in progress" beside "complete, 934
records"). Which copy was current was determined by `git log -S` introduction dates, not
assumption; the latest copy won in every case and no kept row was modified. Fixed in PR
#28 (docs-only, this lane's authority). Root cause matches RULE-REBASE-001's target
failure mode; the rule post-dates the damage.

### F4 — Org-doc letter conflict: RULE-CORPUS-ID-001 vs §3.12 / RES-V1 brief (MEDIUM)

BASORG's RULE-CORPUS-ID-001 (correct: ID uniqueness within-file + new-IDs-against-corpus;
global uniqueness produces 536 false positives on deliberate re-pass/repair records)
contradicts the *literal* text of `ORYN-ORG-STRUCTURE.md` §3.12 and the RES-V1 brief
("uniqueness … against the ENTIRE research corpus"). A verifier following its brief
literally reproduces exactly the false positives the rule prevents. Fix: one-line pointer
in the org doc. Owner: founder (org doc is founder/CEO territory; flagged, not self-fixed).

### F5 — The CFO role is not yet on main (MEDIUM, resolution already in flight)

The org amendment defining ORYN-CFO (rules 14–17, commit `ae67cb8`) exists only on
`oryn/ceo-rule-monotonic` — **open as PR #25**. Until it merges, every session verifying
this lane's authority hits a false start (BASORG and MERGE-1 both did, correctly refusing
to trust a self-claim; RES-V2 found the branch). Action: verify + merge PR #25 through the
normal flow.

### F6 — Glasgow batch2: block correct, headline overbroad, one false-merge trap found
(delivered to BASORG at its request; recorded here)

Re-derived cold from `acquire-programs-batch2_2026-08-20.jsonl` vs live (university_id
`05a25b80…`, identity checked): BASORG's 69/101 duplicates reproduce **exactly on two
independent axes** (suffix-stripped names AND URLs) — but the two 69-sets differ by one
record each way, and 31 records match live on *neither* axis. So: 69 certain duplicates +
~32 genuinely missing official programmes (faster-route/partnership/dual-degree/
graduate-entry variants + Quantitative Methods), not "101 duplicates". The trap case:
`Music [BMus]` name-strips to live "Music" — which is the *MA* programme; a name-based
dedup would merge two different degrees. URL must outrank stripped-name. Additionally,
live Glasgow `degree_type` is NULL ×101 while the file populates 93 — under
RULE-INGEST-003 the 69 are an ENRICH/UPDATE case, joining RES-I1's 1,437 `url_repair_*`
records in the same missing supersede path. BASORG accepted all points, extended RES-V1's
audit to already-ingested batches, and issued the URL-primary constraint.
**Not verified:** the 32 against Glasgow's site itself (file-vs-live only).
**Open item tracked:** the supersede-gap design (owner: RES-I1 via BASORG).

## Verified-clean (checked, no finding)

- **RES-I1 status report:** every claim verified exactly (counts 16,119/1,254/396 at
  14:41; the 5-row apply in the 11:36 UTC window; all three run reports pushed).
  Three independent verifications now agree (RES-I1, BASORG, CFO).
- **Ownership integrity:** every org lane branch active since launch maps to a claimed
  row — five lanes' rows (RES-R1, RES-V2, RES-I1, RES-I2, UI-1) are in flight on their
  own PR branches, which is the documented claim path, not unrecorded work. MERGE-1 has
  no row anywhere, consistent with its no-standing-worktree brief (informational).
- **BASORG spot-checks:** opportunities 391; TechGirls 37 countries; Türkiye Scholarships
  exclusion prose live; PR #12 merged / #27 open — all exact.
- **BASORG's 8 standing rules** (read at `8ef7442`): internally consistent;
  RULE-FETCH-001's `browser_render` verified first-class in the gate enum
  (`lib/acquisition/retrieval-method.ts:34`).
- **ECW2 SQL stranding:** already resolved — `ecw2_verified_apply_2026-08-22.sql` is on
  main; live effects verified.
- **current-state.md:** stale in self-disclosed ways (numbers predate today's CA apply and
  org launch) but stamps provenance and defers to live measurement; its discipline held.
  A checkpoint rewrite is due when the CEO next closes an integration wave (LOW).

## Corrections issued and accepted this round

- BASORG's "org docs aren't on main" was false (landed 13:01, PR #4 merge `38d7be7`);
  root cause was its own stale-local-ref check, which BASORG diagnosed itself, corrected
  to the founder, and ratcheted into a standing rule. Self-caught error: system working.

## Explicitly NOT audited this round (rotation candidates for round 2)

Founder-blocked-backlog completeness; scope-vs-brief diff review of the code lanes
(FEAT-1/FEAT-2/BUG-1/UI-1); MERGE-1's merge-log vs actual merge commits; the pre-org
historical branch backlog (~80 branches, most marked complete/dormant — current-state
already flags this as integration debt); RES-R2/RES-V-R3W2 output content; the
`_backup_*` table cleanup question.
